# CutFEM super-element operator learning — state and measurements, 2026-09-15

Self-contained briefing. All numbers below are measured on our machines unless marked as an
estimate or an inference. Where a quantity is a one-sided bound from an iterative estimator rather
than an exact eigendecomposition, it is labelled.

---

## 1. Scientific setting

**Goal.** Learn a geometry-conditioned neural operator that reproduces the interface Schur
complement of a TPMS (P-type sheet) unit cell, so that predicted modules can be assembled into
lattices and used inside topology optimisation.

**Why static condensation rather than homogenisation.** Homogenisation assumes a complete unit cell
and periodic boundary conditions. Both fail for *cut* cells and *boundary* cells, which is exactly
the regime of interest. Static condensation needs no boundary-condition assumption and is exact
against any neighbour.

**Why CutFEM.** All cells live on one common background mesh, so interfaces between neighbouring
modules match by construction and the condensed operators are composable.

**Pipeline.** Geometry (TPMS level set + cut plane) → CutFEM discretisation on a 65³ background
grid, Q2 elements, n = 32 → body stiffness → eliminate interior dofs → Schur complement S on the
cell's complete trace (all dofs on the cell boundary) → remove 6 rigid modes → the label.

---

## 2. The label object

For each identity (unit cell geometry):

- `q` = complete trace dimension (number of trace dofs). Population: 1024 identities, q ranges
  roughly 3 000 – 47 760. Example used throughout: seat 0328, q = 12798, 4266 trace nodes × 3
  components.
- `B` = orthonormal rigid quotient (6 Householder reflectors), maps trace → quotient, `d = q − 6`.
  For 0328, d = 12792.
- `A = B S Bᵀ` = the quotient operator, SPD.
- **The stored label is `R*`**, the upper-triangular Cholesky factor of `A`, stored row-packed in
  float64. `A = R*ᵀ R*`. Full trace reconstruction is `S = Bᵀ R*ᵀ R* B`.

Label sizes: d(d+1)/2 float64. 0328 is 654 MB; the largest identity would be ~9 GB.

---

## 3. The teacher and its validation status

The teacher is CutFEM **with ghost penalty (GP)** — a stabilisation term penalising jumps of normal
derivatives across faces of cut cells, coefficients γ = η = 1e-4 in our compilation.

A separate line of work (parallel agent, different machine) validated the GP teacher against an
**independent physical reference**: a separately generated body-fitted surface + Tet10 volume mesh,
not using the teacher's G, S or GP energy. Compliance comparison on load spaces:

| what | GP vs finer reference, max compliance difference | reference's own 2-level mesh change |
|---|---:|---:|
| 4 single cells, 6 rigid-fixture load directions | 0.084% – 0.101% | 0.130% – 0.273% |
| 0328, 12 independent finite-area local tractions | 0.253% | 0.502% |
| 2-cell, 2×2×2, 1×1×4 assembled FULL structures | 0.052% – 0.090% | 0.108% – 0.190% |

GP coefficient sensitivity, same load spaces: halving γ changes compliance by 0.007% (6-dim) /
0.020% (12-dim); doubling it by 0.014% / 0.039%.

Assembly consistency (module Schur complements summed on shared dofs vs a direct FE solve of the
union body; each module keeps its own intra-module GP, no cross-module GP added):

| structure | body / trace dofs | displacement rel. diff | compliance max diff |
|---|---|---:|---:|
| two cells | 460 176 / 23 388 | 1.27e-12 | 5.63e-11 |
| 2×2×2 | 1 823 760 / 76 608 | 3.82e-13 | 6.75e-12 |
| 1×1×4 | 918 264 / 44 688 | 1.01e-11 | 6.46e-10 |

Geometry sensitivity along one path: scaling all τ corner thicknesses by (1+ε), central difference
at h = 1e-3 vs 1e-4, whitened by the baseline load compliance → relative Frobenius difference
0.0231%. Nodes and GP faces did change across that perturbation.

**Explicitly not established** (stated by that report): no full-trace stiffness-spectrum comparison
against the independent reference; only FULL (unmirrored) structures assembled, no general oblique
CUT structures; one sensitivity path only, no independent physical gradient; 3 of 16 fixed
identities have unclosed topology rebuilds on that host.

Background: an earlier diagnostic found that a 0.01% thickness perturbation changed the overall
matrix by 0.0234% but changed one complete-boundary direction's stiffness by 59.76%, with 11
directions over a 1.1 stiffness ratio; that perturbation activated a background cell of material
volume fraction ~5.6e-15 and three new GP faces. A controlled rewrite of that stabilisation energy
reduced the neighbouring-geometry max stiffness ratio from ~1.598 to ~1.016.

---

## 4. The learning problem and its metrics

Student produces an operator `Â`. Whitened spectrum `μ = eig(R*⁻ᵀ Â R*⁻¹)`. Exact log-det
divergence:

```
D = tr(A⁻¹Â) − log det(A⁻¹Â) − d = Σᵢ (μᵢ − log μᵢ − 1) ≥ 0,   zero iff Â = A
```

Reported per mode as `D/d`. Registered gates: **work gate** μ within ±10% on every direction;
**target gate** μ within ±3% and D/d < 1e-4.

In the production code D is computed in closed form (trace identity + log-det with a rank-6 rigid
correction), never by forming the spectrum. A `solve_tol` of 1e-6 on the rigid-correction solve
residual marks a step as numerically unreliable; those steps drop the rigid log-det term.

**Prior state of the neural side.** A single-geometry, free-complete-factor, 100-step run
("N1") ended at D/d = 6.84, μ ∈ [2.27e-4, 379], 9942 of 12792 directions below 0.9, 2671 above
1.1; both gates failed. A separate arithmetic check ("N0": load the true factor, replay) closes at
~1e-12, i.e. the representation reproduces the teacher when handed the exact coefficients.

---

## 5. Measurement A — free complete factor, one fixed label (seat 0328)

Free upper-triangular `R̂`, no geometry front-end, teacher available. Closed form used:

```
D(R̂) = ‖R̂ R*⁻¹‖²_F − 2 Σᵢ log R̂ᵢᵢ + log det A − d
∇D    = 2 R̂ A⁻¹ − 2 diag(1/R̂ᵢᵢ)      (upper triangle only)
```

D is convex in `Â = R̂ᵀR̂` (trace linear, −logdet convex), unique minimum at `Â = A`.
Evaluator self-check: `D(R*) = 0.000000e+00`, trace term = 12792.000000 = d, exactly.

Stronger, and purely algebraic from the same formula (contributed by an outside review of an
earlier draft of this document, verified here): on the domain of upper-triangular matrices with
positive diagonal, D is **strictly convex in R̂ itself**, since for any nonzero upper-triangular ΔR

```
d²D(R̂)[ΔR, ΔR] = 2‖ΔR R*⁻¹‖²_F + 2 Σᵢ (ΔRᵢᵢ)² / R̂ᵢᵢ²  >  0
```

(the first term because A⁻¹ = R*⁻¹R*⁻ᵀ, the second from −2Σlog R̂ᵢᵢ). Hence R* is the unique
stationary point and the earlier failure cannot be a local minimum of this parameterization. This
statement is confined to the free raw factor: it does not extend to the log-diagonal
parameterization, to the (L, M) parameterization of §7, or to shared network parameters.

### A.1 Starting points

| start | D/d |
|---|---:|
| 0.01·I (what N1 used) | 7.44 |
| c*·I, c* = √(d / tr A⁻¹) = 4.13e-3 (optimal scalar) | 4.35 |
| diag(R*) (the teacher's exact diagonal) | 5.24 |
| diag(√Aⱼⱼ) | 7.34 |

tr(A⁻¹) = 7.49e8.

### A.2 Curvature of the parameterization

```
∂²D / ∂R̂ᵢⱼ² = 2 (A⁻¹)ⱼⱼ        (+ 2/R̂ᵢᵢ² on the diagonal)
```

i.e. it depends only on the column index j. `(A⁻¹)ⱼⱼ` is the compliance of quotient coordinate j.

The table below is the spread of the **Hessian's diagonal entries**, not the condition number of
the full Hessian. In vec form the quadratic part's Hessian is 2(I ⊗ A⁻¹), whose condition number is
cond(A); scaling column j by 1/√((A⁻¹)ⱼⱼ) is Jacobi preconditioning of A⁻¹, which equalises the
diagonal but does not decouple the coefficients. A residual diagonal spread of ~10 therefore does
not by itself establish that the problem is easy to optimize.

| | min | max | spread |
|---|---:|---:|---:|
| raw, off-diagonal columns | 1.10e3 | 2.96e6 | 2.70e3 |
| raw, diagonal | 1.20e3 | 5.82e6 | 4.87e3 |
| log-diagonal reparameterization | 4.00 | 4.34e1 | 1.09e1 |

Teacher's diagonal: min 8.29e-4, max 1.42e-1, ratio 1.71e2.
Column scale `1/√((A⁻¹)ⱼⱼ)`: min 8.23e-4, max 4.27e-2, spread 51.9.

### A.3 Parameterization comparison — same start (c*·I), same optimizer, same lr

1000 Adam steps, lr 1e-3, float32 arithmetic, D evaluated in float64:

| parameterization | D/d at 1000 | μ (svdvals of R̂R*⁻¹, exact) |
|---|---|---|
| raw | NaN before step 100 | [2.45e-5, 1.71e4] |
| log-diagonal only | 4.85 (non-monotone: 4.32 → 4.66 → 5.12 → 4.85) | [2.90e-9, 1.61e4] |
| column scaling (column j ÷ √((A⁻¹)ⱼⱼ)) | 1.25, monotone; 0.081 at 4500 steps | [1.17e-3, 1.68e1] |
| Newton direction −½∇A, fixed lr 0.5 | NaN | — |
| Newton direction + Armijo backtracking | 0.026 at 60 steps | [0.043, 22.4] |

### A.4 Newton + Armijo, run to the gate

228 steps, 25 min on one RTX 5090, float32 steps:

```
step 100  D/d 4.54e-3
step 150  D/d 5.87e-4
step 200  D/d 6.72e-5
step 228  D/d 2.11e-5   ← line search returns t = 0 (float32 arithmetic floor)
```

Final exact spectrum (svdvals): **μ ∈ [0.9558, 1.0468]; 0 modes below 0.9; 0 above 1.1;
12709 of 12792 (99.35%) inside ±3%.**

So the **work gate (±10%) is passed and the target gate (±3% on every direction) is not**: the
extreme deviation is 4.68% and 83 directions lie outside ±3%. Note that D/d = 2.11e-5 is already
below the 1e-4 threshold while the spectrum gate still fails, i.e. a per-mode average divergence
does not substitute for the all-direction gate.

Typical accepted line-search step was t ≈ 0.0078 (the full Newton step of the quadratic part
overshoots the log barrier on the diagonal).

---

## 6. Measurement B — where can the column scale come from?

Both methods in A use the teacher (`diag(A⁻¹)` or `A`). A network has neither at prediction time.

### B.1 Self-preconditioning

Column scale refreshed every 100 steps from the student's own `diag(Â⁻¹)`; Adam's first and second
moments rescaled into the new coordinates at each refresh (a first run omitted that rescaling and
was discarded). 3000 Adam steps, lr 1e-3, same start.

| scale source | D/d at 3000 | log-correlation of scale with the teacher's |
|---|---:|---:|
| teacher `diag(A⁻¹)` | 0.289 | 1.000 |
| teacher start, then self-refresh | 1.248 | 0.963 |
| flat scale (all ones) start, then self-refresh | NaN | 0.994 (last finite refresh) |

This falsifies the tested self-refresh configuration only. It does not bear on other uses of
teacher-derived information: a scale that appears in the forward map `R̂(g) = F_θ(g)·C(g)` must be
computable from geometry alone at inference, but a teacher-derived preconditioner acting on the
loss metric or on the parameter space during training need not survive to inference. No experiment
here tests that second route.

### B.2 Regression of compliance on local geometry

Target: compliance per **physical trace dof**, `c_k = ‖column k of R*⁻ᵀB‖²` (so it indexes nodes
and components directly). For 0328: c ∈ [1.98e2, 1.48e6], spread 7.48e3.

The coordinate mismatch with §5 has since been measured and is no longer open; see B.3.

Features, local only: log support weight w (material support of the node's Q2 basis, clipped to
[1e-4, 1]), node position, box-face id (one-hot), near-field degree (number of pairs within radius
0.2), signed distance to the cut plane and its absolute value, 5³-neighbourhood means of the 11
background grid channels, and a 3-way one-hot for the component. Held out: 20% of nodes, all three
components of a held-out node held out together. Ridge regression.

The reported statistic is the curvature spread left after dividing by the prediction (raw 7.48e3;
~10 would let one learning rate serve all columns).

| model | R² (test) | residual spread | residual spread, 5–95% | as column scale (√) |
|---|---:|---:|---:|---:|
| constant | 0 | 7.32e3 | 1.56e3 | 85.6 |
| log w alone | 0.867 | 6.92e2 | 2.07e1 | 26.3 |
| ridge, linear | 0.914 | 4.54e1 | 1.21e1 | 6.7 |
| ridge, quadratic | 0.981 | 1.44e1 | 3.14 | 3.8 |
| 4-nearest-neighbour spatial average | 0.690 | 9.14e3 | 1.48e2 | 95.6 |

### B.3 The quotient map, measured

The quotient is `B = E_{6:} H₆ … H₁ P`: the stored permutation `order`, six dense Householder
reflectors, then drop six rows. So quotient coordinate `j` is physical dof `order[j+6]` — the
index map that B.2 assumed was missing is explicit. On 0328, `‖BBᵀ − I‖_max` = 6.11e-15, the
largest entry of row `j` sits at `order[j+6]` for 100% of rows, that entry is 0.99937–0.99974,
and the off-peak row energy is 5.27e-4 to 1.25e-3.

The **values** still differ, and by more than a relabelling can fix:

| ratio | min | max | spread |
|---|---:|---:|---:|
| `s_j = (A⁻¹)_jj` alone | 5.48e2 | 1.48e6 | 2.697e3 |
| `s_j / c_{order[j+6]}` | 0.885 | 3.628 | 4.098 |
| `s_j / (B∘B c)_j` | 0.877 | 1.022 | 1.165 |
| best over all relabellings of c | 0.759 | 2.746 | 3.616 |

The 1e-3 of off-peak row energy lands on directions whose compliance is up to 7e3 larger, so it
carries roughly 45% of the value. The diagonal pullback `(B∘B) c` accounts for it exactly. `B`
depends only on node positions and the ordering, so the pullback is available at inference.

**Within 0328, 20% of nodes held out**, preconditioned curvature spread `s/ŝ`:

| model | spread | 5–95% | R² |
|---|---:|---:|---:|
| constant | 2646.6 | 607.7 | 0.000 |
| regress `log s` directly, quadratic | 13.3 | 2.77 | 0.979 |
| regress `log c` then pullback, quadratic | 10.9 | 2.39 | 0.986 |

**Leave-one-seat-out over six seats** (the held-out geometry is never seen):

| held out | raw | direct, quad | pullback, quad | 5–95% (pullback) | oracle |
|---|---:|---:|---:|---:|---:|
| 0328 | 2.70e3 | 25.1 | 19.9 | 3.10 | 1.165 |
| 0253 | 2.94e3 | 25.6 | 20.3 | 3.29 | 1.175 |
| 0403 | 1.64e4 | 23.6 | 16.7 | 3.32 | 1.206 |
| 0347 | 1.18e4 | 17.8 | 13.7 | 3.48 | 1.133 |
| 0974 | 1.01e4 | 19.9 | 16.8 | 2.81 | 1.125 |
| 0575 | 1.13e4 | 156.0 | 65.5 | 3.08 | 1.133 |

Linear features reach only 109–316 on the pullback route. Seat 0575's 65.5 sits against a
5–95% core of 3.08, so it is a few extreme coordinates rather than the body.

Six seats, `d` from 12792 to 16122, one cut-plane family, ridge on 28 hand-built features with a
quadratic expansion. Whether this transfers across topologies is untested, and the spreads say
the learning rate can be shared, not that the factor fit succeeds.

---

## 7. Measurement C — capacity of a local operator class (in progress)

The class the existing network head produces:

```
Ŝ = Lᵀ (I + M Mᵀ)⁻¹ L
```

- `L`: upper triangular, nonzeros only on 3×3 node-diagonal blocks and on node pairs within
  radius r = 0.2 in unit-cube coordinates. For 0328 that is ~3.5M free entries.
- `M`: (q × k) dense, k = 1500 here. 19.2M free entries.
- Equivalently `Ŝ⁻¹ = L⁻¹L⁻ᵀ + (L⁻¹M)(L⁻¹M)ᵀ`: a local compliance plus a low-rank addition. Note
  the low-rank term can only make directions *softer*, never stiffer.

**This run removes the network's head bounds** (free entries, diagonal via exp) and applies the
column scaling from A, so it measures the class's capacity rather than the network's feasible set.
Start: `L = diag(1/√c_j)`, M ≈ 0. Adam, lr 1e-3, 22.8M parameters, 0.41 s/step.

| step | D/d | μ_min (upper bound) | μ_max (lower bound) | cond(L) | ‖M‖₂ |
|---:|---:|---:|---:|---:|---:|
| 0 | 1.1194 | 0.0172 | 275.07 | 8.5e1 | 0.15 |
| 1000 | 0.0477 | 0.0732 | 1.195 | 5.06e6 | 10.85 |
| 2000 | 0.0057 | 0.240 | 1.235 | 5.14e6 | 13.72 |
| ~2400 | 0.0036 | — | — | — | — |

μ bounds here are one-sided estimates from block power iteration, not an exact eigendecomposition.
Their direction is nonetheless sufficient to establish failure at step 2000: μ_min ≤ 0.240 exhibits
an over-soft direction and μ_max ≥ 1.235 an over-stiff one, so neither gate is met there. The
converse does not hold — a one-sided bound inside a gate would not establish that the gate is met.
At step 2000: σ_max(L) = 2.07e-1, σ_min(L) = 4.02e-8, bound on cond(K0) = 4.99e15.
17 of the first 1600 steps exceeded the 1e-6 rigid-solve tolerance and dropped the rigid log-det
term. Run continues to 3000 steps.

**R1 end state and why it is superseded.** The run above reached a 200-step mean D/d of about
0.0039 near step 2960 before being stopped. Over its whole trajectory **18 steps exceeded the
rigid-solve tolerance**, and in that code path `divergence_terms` returns the objective with its
rigid log-det term removed while the optimizer still takes the step. Those updates therefore
optimized a different function, so this trajectory can show that the class reaches this magnitude
but cannot be used to attribute the remaining error between capacity and optimization.

**R2 through R7: four stalled runs and what they turned out to be measuring.**

R2 added a rejection rule: a step whose rigid-correction residual exceeds `--solve-tol` is undone,
the learning-rate multiplier halved, and the rejection counted, so that every update comes from the
complete objective. Runs since:

| run | `--solve-tol` | refinement | outcome |
|---|---:|---:|---|
| R2 | 1e-6 | none | stalled step 281, 45 rejections, residual floor 1.03–1.08e-6 |
| R4 | 1e-6 | 4 | 8 rejections by step 208, residual floor ≈1.1e-6 |
| R5 | 1e-4 | 4 | stalled step 197, 427 rejections by step 710, residual 1.03–1.19e-4 |
| R7 | none (rejection off) | none | still running; residual 1.3e-15 → 7.2e+29 by step 300 |

The residual floored just above whatever tolerance was configured, which read as the tolerance
being the binding constraint. R7, with the rule off, shows it was not: the residual is not
drifting into a floor, it is increasing monotonically by 44 orders of magnitude in 150 steps,
while D/d descends smoothly throughout.

| step | rigid solve residual | `logdet(NᵀŜ⁻¹N)` | D/d |
|---:|---:|---:|---:|
| 25 | 1.25e-15 | 111.229 | 0.912 |
| 150 | 1.84e-14 | 112.787 | 0.558 |
| 175 | 5.96e-13 | 130.576 | 0.524 |
| 200 | 1.23e-03 | 227.878 | 0.486 |
| 250 | 4.22e+16 | 469.415 | 0.408 |
| 300 | 7.24e+29 | 656.823 | 0.345 |

The run log carries both halves of `gap = logdet Ŝ + logdet(NᵀŜ⁻¹N) − logdet(NᵀN) − logdet A`.
Relative to step 50:

| step | Δ logdet Ŝ | Δ logdet(NᵀŜ⁻¹N) | implied rigid | implied non-rigid | per non-rigid mode |
|---:|---:|---:|---:|---:|---:|
| 200 | +1434.3 | +116.6 | −116.6 | +1550.9 | +0.1212 |
| 250 | +2616.1 | +358.2 | −358.2 | +2974.2 | +0.2325 |
| 300 | +3681.2 | +545.6 | −545.6 | +4226.8 | +0.3304 |

Six eigenvalues carry −545.6 of log-determinant, so each has shrunk by `exp(−545.6/6)` = 3.2e-40,
while the other 12792 have grown by `exp(0.33)` = 1.39× each. The student is converging toward an
operator singular on the rigid subspace — which is what the true Schur complement of a free cell
is.

The parameterization `Ŝ = Lᵀ(I+MMᵀ)⁻¹L` with `L` nonsingular has no rigid null space available,
so it approaches one by sending those six eigenvalues toward zero. The identity used to obtain
`logdet Â` routes through exactly them, with the rigid contribution appearing in both terms and
cancelling. The operands therefore diverge as the fit improves, and `NᵀŜ⁻¹N` becomes a solve
against condition number ≈1e40. The `--gap-audit` instrumentation shows iterative refinement
diverging there (refine-8 residual 9.5e+146 at step 300) and the rigid log-det moving 1551 between
refine 0 and refine 8, against a `D` of about 4400. So past roughly step 225 the objective's value
and gradient are not trustworthy.

Two observations that may or may not point the same way:

- In exact arithmetic `D` is invariant to Ŝ's rigid block and its rigid-to-quotient coupling:
  `Â = BŜBᵀ` sees neither, and the trace term is built from `Z = R*⁻ᵀB` whose columns lie in the
  same complement. So the collapse is motion along a flat direction of the true objective. Why it
  is monotone rather than diffusive is not established; one candidate is that `logdet Ŝ` is read
  exactly off the diagonals of `L` and `C` while `logdet(NᵀŜ⁻¹N)` comes from a degrading solve, so
  the cancellation leaves a small spurious descent.
- The rejection rule was detecting something real, but its response could not address it: halving
  the learning rate does not change a solve residual, so once triggered it could only trigger
  again. R5 rejected 512 consecutive steps.

An earlier version of this briefing reported an audit suggesting the residual was harmless. That
audit was placed one line after `opt.step()`; `FreeScaledModel.forward` returns its `M` parameter
directly, so the operator held post-step `M` against a Cholesky factor built from pre-step `M`, and
the audit measured that inconsistency. Its numbers are withdrawn.

**Untested candidates**, listed without a recommendation: evaluating `logdet(BŜBᵀ)` directly from
`W = LBᵀ` and a Cholesky of `Wᵀ(I+MMᵀ)⁻¹W` (exact, no rigid inverse anywhere, roughly `d²q + d³/3`
≈ 2.7e12 flops per step against the current 2.1 s); projecting the rigid modes out of the model so
there is nothing left to collapse (leaves the log-det still needing another route); and pinning the
gauge by holding `ŜN` at its initial value, one six-column application per step, whose risk is that
a banded `L` may not reach the wanted `Â` while holding a fixed rigid block. The read-out for the
last of these would be D/d at steps 100/150/200 against R7's 0.628/0.558/0.486, measured while
R7's arithmetic was still clean.

**Reference point for the same class without refitting**: truncating the teacher's own Cholesky
factor to the same radius-0.2 band (computed 2026-09-12, three elimination orders — packet, RCM,
raster — agreeing) drops only 2.5–2.6% of the factor's Frobenius norm and gives D/d = 0.030–0.032,
μ ∈ [0.092, 86–88], with ~294–300 directions more than 10% off. Adding a rank-k softening
correction to that truncation brings the over-stiff side to μ_max = 1.016 / 1.0038 / 1.0011 at
k = 512 / 1500 / 3000; the over-soft side (μ_min ≈ 0.09) is not reachable by softening.

**The network's actual head bounds**, for reference (these were *not* applied in the run above):

```
L diagonal   = √(S0·w) · exp(3·tanh(dg/3)),  S0 = 0.01, w the node support weight
L off-diag   = tanh(·) · min(D_i, D_j) · off_scale · exp(−dist/decay),  off_scale = 10, decay = 0.03
M            = 0.1·tanh(·) · exp(logscale)   [in the bounded variant]
```

---

## 8. Measurement D — cost of a multi-level elimination tree

Symbolic only: recursive bisection of the cell set on the widest centroid axis, separator sets read
off the incidence pattern, dense frontal model `e³/3 + e²b + eb²`, leaf 8 cells. Five no-GP bodies.

| body | cells | coords | trace | trace% | tree flops | monolithic | ratio | peak front |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| input_0693 | 14 | 693 | 552 | 79.7% | 1.774e7 | 5.487e7 | 3.1× | 0.002 GiB |
| input_0448 | 19 | 891 | 711 | 79.8% | 1.917e7 | 1.160e8 | 6.0× | 0.004 GiB |
| input_0269 | 68 | 2757 | 2088 | 75.7% | 4.058e8 | 3.951e9 | 9.7× | 0.034 GiB |
| input_0010 | 250 | 8991 | 5433 | 60.4% | 1.477e10 | 1.888e11 | 12.8× | 0.241 GiB |
| input_0245 | 391 | 13287 | 7080 | 53.3% | 3.489e10 | 6.636e11 | 19.0× | 0.420 GiB |

Root flop share rises 15.7% → 32.2% → 36.0% → 52.7% → 65.4%; leaf share falls 84.3% → 1.1%.

Two limits. The profiled bodies are trace-dominated (53–80%) while production bodies are about 13%
(≈12800 trace of ≈100000), and root flops carry `b` equal to the whole trace, so these root shares
are upper bounds rather than production estimates; the direction is certain, the magnitude is not
measured. And absolute cost is small at these scales — 3.49e10 flops for the largest whole tree —
so the binding cost is the `O(q²)` deliverable, which no tree removes: the root front is `(e+q)²`,
about 1.3–1.6 GiB at production `q`, the same order as the Schur complement itself.

GP labels carry only `S_UPPER.npy`, `TRACE.npz`, `PROBES.npz`, so a GP-teacher tree would need the
body recompiled from the frozen source. GP adds face-coupling edges and so increases fill.

---

## 9. Hardware and cost

One RTX 5090 (32 GB), 16 CPU cores, 90 GB RAM. Seat 0328 at d = 12792: dense d×d float64 is
1.31 GB; the free-factor Newton run peaked around 10 GB; the local-operator run peaks at 9.4 GB.
228 Newton steps = 25 min; 3000 Adam steps on the local class = 20 min.

---

## 10. Open questions

Listed without a preferred answer.

1. B.3 now measures the preconditioner route end to end: on a geometry never seen, predicting log c
   from local geometry and applying the exact (B∘B) pullback leaves a curvature spread of 14–20
   against an oracle floor of 1.13–1.21. Whether a spread of ~15 is sufficient in practice is still
   untested — it says one learning rate can serve all columns, not that the fit succeeds. Six seats,
   one cut-plane family, ridge on hand-built features rather than a network.

2. The local class reaches D/d ≈ 0.0036 so far with the head bounds removed, versus 0.030–0.032 for
   a truncation of the teacher's own factor and 2.11e-5 for a free dense factor. The run has not
   converged and the bounded-head version has not been run, so the gap attributable to the head
   bounds is unmeasured.

3. During that run cond(L) rose from 8.5e1 to 5.1e6 with σ_min(L) = 4.0e-8. R7 now suggests part of
   this is the rigid collapse described in §7 rather than anything the quotient fit needs, since the
   six rigid eigenvalues fall by 3.2e-40 while the other 12792 grow only 1.39×. How much of the
   cond(L) growth is gauge and how much is the fit is not separated, and the bounded head has still
   not been run.

3b. Whether an operator fitted under the corrupted objective past step 225 is nonetheless a good
   operator is measurable and not yet measured: R7's dense evaluation at step 1000 computes the
   whitened spectrum independently of the running objective.

4. A dense Cholesky factor's column j depends on all coordinates preceding j in the elimination
   order, which under Morton ordering is a spatial region rather than a neighbourhood; and a
   geometry change that shifts the ordering rewrites part of the factor. Whether this makes the
   complete factor unsuitable as a *network output* (as opposed to as a *stored label*) has not
   been tested directly — the evidence is indirect (the truncation numbers above, and the curvature
   result in A.2).

5. Nothing here addresses generalisation. All of A, B and C fit one fixed label with the teacher
   available. No geometry front-end has been attached, and no cross-geometry training has been run
   under the corrected parameterization.

6. On the teacher side, the items in §3 marked "not established" remain open, in particular the
   full-trace spectrum comparison against an independent reference, and assembly of general oblique
   CUT structures rather than FULL cells.
