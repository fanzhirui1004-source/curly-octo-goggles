# 晨报 2026-09-23

详细数据见 `docs/NIGHT_REPORT_20260923_CN.md`，证据和源码见 `docs/data/takeover_20260923/`。
门槛没有放宽：目标是 [0.97, 1.03] 且 D/d < 1e-4。下文的“见证”是有限方向的最坏值，“全谱”是全部方向。

## 一句话结论

**“几何只编码一次 → 可复用的结构化算子 → 任意边界位移快速返回完整边界反力”，今晚在 GPU 上从头到尾跑通了
（编码 1–25 s，单次查询 0.5–29 ms），
而且完全不依赖教师产物：从几何参数出发生成的算子，在第二批 6 个训练几何上与教师标签相差 0.2%–0.65%。
整条链路里没有需要学习的环节。**

## 链路和每一环的状态

| 环节 | 做法 | 是否依赖教师产物 | 精度 | 目前耗时 |
|---|---|---|---|---|
| 单元刚度 | 子网格线性化水平集 + 八叉树加密 → 精确积分 125 个矩 → 固定弹性模板 | 否 | 整体见证 ≤ 1.0065 | GPU 1–5 s |
| GP | 冻结代码选面 + 固定模板 | 否 | 与生产机逐一相同 | 5–11 s（Python） |
| 迹坐标 P | 冻结代码 compile_trace | 否 | 与教师完全相同 | 1–216 s（Python，要优化） |
| 迹编译 PᵀKP | 稀疏矩阵乘，得到内部块 A、耦合 C、边界块 D | 否 | 精确 | 0.03–10 s（大样本在 CPU 上，要搬到 GPU） |
| **结构化算子** | **GPU 稀疏 Cholesky 因子（cuDSS）**，编码时规划和分解一次 | 否 | 精确（残差 1e-15） | 规划 0.02–4.8 s + 分解 0.01–3 s |
| **查询** | S·q = D q + Cᵀz，z 由因子前代回代得到 | — | 精确 | **1 列 0.5–29 ms，64 列 1.4–233 ms** |

（原来的做法是 GPU 求解器层级加自适应 Chebyshev，精度相同，但查询要 0.4–54 s；保留作备选。）

## 证据

**第二批，完全不依赖教师产物，与教师标签比较（有效分辨率 8）：**

| 样本 | 保留体积 | τ 场 | 见证 |
|---|---|---|---|
| fresh_train_0011_cover01_r1 | 0.008 | 仿射 | 1.0021 |
| fresh_train_0008_cover01_r1 | 0.070 | 混合 | 1.0059 |
| fresh_train_0013_cover01_r1 | 0.148 | 混合 | 1.0065（有效分辨率 16 时 1.0016） |
| fresh_train_0026_cover01_r1 | 0.305 | 仿射 | 1.0028 |
| fresh_train_0007_cover01_r1 | 0.336 | 均匀 | 1.0061 |
| fresh_train_0021_cover01_r1 | 0.758 | 仿射 | 1.0036 |

配上精确单元刚度时，这 7 个样本的探针反力与教师吻合到 1e-12 以内，说明 P、GP 和坐标全部正确。

**第一批（多面体积分单元 + 教师的 P 和 GP）：**

| 样本 | 类型 | 见证 | 严格全谱 |
|---|---|---|---|
| train3_d0_v0 | 重度 | 1.0024 | 分辨率 4 时 [0.9959, 1.0107] |
| train13_d0_v1 | 中度，切面薄片最多 | 1.0064 | **[0.99923, 1.0064]**（PARDISO 精确） |
| train3_full | 完整单胞 | 1.0034 | — |
| train6_d1_v1 / train6_full | 普查中单元偏差最大 | 1.0042 / 1.0044 | — |
| dev0000_d0_v0 | 开发样本，最难之一 | 1.0056 | — |

**求解器本身（精确单元）：**
- 配对平滑器让中度样本从不收敛（12.6）变为全谱 [1, 1.0013]。
- 不加配对平滑器时，6 个开发样本里有 4 个在 64 层后仍为 4.2 到 9.2 倍。
- 自适应深度下，开发样本 dev0006 的全谱为 [1, 1+7e-9]。

## 为什么单元刚度不需要网络（这是方向性的结论）

1. 单元刚度只要在**自身能量范数下误差 ≤ ε**，整体就保证在 ±ε 以内（理论和实测一致）。
2. 但回归式的误差会被放大：单元矩阵逐条目只错 1%，整体就会失去正定；单项式矩只错 1e-4，整体就超出 ±14%。
   **直接回归单元矩阵或矩的网络，基本达不到要求。**
3. 确定性的几何积分天然给出半正定、刚体零空间精确的单元，误差按二阶收敛；
   GPU 上一个完整单胞约 1 万个单元，1 到 5 秒算完。
4. 单元级普查：第一批 111 个、第二批 21 个样本。大单元里最差的单元偏差可达 18.8%，
   但凡是做过整体测试的样本，整体误差都在 0.65% 以内，因为影响整体的是占绝大多数、偏差 <1% 的单元。

## 时间

**端到端（从几何参数出发，单元有效分辨率 8，GPU 直接分解）：**

| 样本 | 编码总时 | 查询 1 列 / 64 列 | 见证 |
|---|---|---|---|
| cover0011（保留 0.008） | 0.96 s | 0.5 / 1.4 ms | 1.0021 |
| 重度 train3_d0_v0 | 1.7 s | 2.3 / 18 ms | 1.0024 |
| cover0008（0.070） | 2.9 s | 2.4 / 18 ms | 1.0059 |
| cover0013（0.148） | 6.0 s | 6.5 / 53 ms | 1.0065 |
| cover0026（0.305） | 7.0 s | 5.8 / 45 ms | 1.0028 |
| 中度 train13_d0_v1 | 7.4 s | 15 / 124 ms | 1.0064 |
| cover0007（0.336） | 12.3 s | 13 / 108 ms | 1.0061 |
| cover0021（0.758） | 18.1 s | 17 / 136 ms | 1.0036 |
| 完整单胞 train3_full | 25.4 s | 29 / 233 ms | 1.0034 |

编码时间不含 P 和 GP 的生成（冻结的 Python 代码，另计 1–216 s）。编码里剩下的大头：
CPU 上的 PᵀKP（最多 10 s，可以搬到 GPU）和 cuDSS 的重排序规划（最多 4.8 s）。
作为对比，教师管线仅求积一步就要 18.9 s（重度）到 185 s（完整单胞，CPU）。

**三种查询方式的对比（教师单元，只比求解器）：**

| 样本 | Chebyshev（GPU，约 186 层）1 / 64 列 | CPU PARDISO 分解；回代 1 / 64 列 | GPU cuDSS 规划 + 分解；查询 1 / 64 列 |
|---|---|---|---|
| 重度 | 0.42 / 3.5 s | 1.5 s；0.05 / 0.45 s | 0.4 + 0.03 s；2 / 17 ms |
| 中度 | 1.7 / 32 s | 9.0 s；0.52 / 7.1 s | 3.0 + 1.5 s；16 / 133 ms |
| 完整单胞 | 2.8 / 54 s | 17 s；0.88 / 12.6 s | 4.9 + 3.1 s；28 / 227 ms |

## 需要你拍板的事

1. **神经网络还要不要、放在哪里。** 精度这条腿已经不需要网络；查询这条腿也不需要了：
   GPU 稀疏 Cholesky 让查询变成毫秒级而且精确，比 Chebyshev 快 100 到 240 倍（按编码器里 8 列一块的实测）。
   原本设想的“学一个更强的平滑器把 186 层压到几十层”，即使做成也比直接分解慢两个数量级，**不建议再训练加速网络**。
   我的建议：“可复用的结构化算子”就定为 {P、GP、A/C/D、cuDSS 因子}，查询做前代回代。
   如果还要用网络，唯一剩下的位置是替代慢的 Python 环节（compile_trace、GP 选面），但这些是确定性代码，改写比学习更合适。
2. **第二批数据的用途。** 原计划用它训练单元网络，现在单元不需要学。
   建议改作验证集：36 个几何，覆盖保留体积 0.008–0.99，本机已补齐单元数据并逐位对账。
3. **下一步工程**，按收益排序：
   - compile_trace 提速（最慢 216 s）；
   - GP 选面向量化；
   - PᵀKP 搬到 GPU；
   - cuDSS 重排序规划提速（只依赖稀疏结构）；
   - 在更多第二批样本上跑完整的端到端全谱。

## 还没收尾的

- 第二批 fresh_train_0015_cover01_r1（保留体积 0.91）编码时显存不足：单个进程用到 30 GB，原因待查。
  同规模的完整单胞 train3_full 能跑通。
- 第一批普查有 5 个样本失败，待查。
- dev0002 的两个自适应有限见证因为当时显存冲突失败，队列里在补跑。
- 第二批 G 阶段：36 个样本中 35 个逐位对账吻合，剩下 1 个待查。
