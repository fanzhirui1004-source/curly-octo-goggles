# 今晚任务清单（2026-09-24 夜，用户已同意）

目标：在 0020 这组几何（重度切割 r1，保留 0.195；中度切割 r2，保留 0.586；它们共用的 FULL 母胞）上，得到 v0 神经算子的第 1 步判决结果。

## 任务
- T0 审定架构：设计流程（wf_3586e554-57e）出结果后，逐条核对设计文档第 2 节的已定决定，写中文摘要存入设计文档。
- T1 补完第 0 步：
  - 晶格验收装置：x、y 两种构型；6 个盒面载荷计入验收门，3 个裁切面载荷只报告；输出柔度、各胞能量、角点灵敏度；被测算子只通过“给 q 出场”的接口接入；先用教师和一个故意做粗的算子核验装置本身。
  - q 样本库：四类配比，每个几何分训练 / 验证 / 测试，附精确能量作归一化。
  - 网络输入数据：节点、单元连接、掩码、单元矩、弱节点、粗网格映射。
- T2 零参数基线（不学习的延拓），量出网络要补的差距；实现 v0 并做单元测试（线性、端口、刚体、等变、单双精度读出）。
- T3 第 1 步训练：三个几何各自训练，重度切割胞先做；能量损失按精确能量归一，热身后加对抗最坏方向；定期过验收装置。
- T4 诊断与联合训练：过门的几何做三几何联合训练；不过门的按“训练信号 → 容量 → 全局传力”的顺序查。v0 不好时可以改架构，例如拆成几个流程块，逐块定位哪一块不行（用户授权，具体怎么改由 Claude 决定）。
- 贯穿全程：每约 2 小时更新进度文档并推送；早上交中文报告。

## 防止跑偏
1. 今晚的对象是神经网络。精确求解只用于教师（标签、归一化、受力驱动样本）和验收参照；不给精确求解器提速，网络前向里不放精确分解。
2. 只有学出来的模型算进展；混入精确部件的候选单独标明“非学习”。
3. 学习部件失败时，修它的参数化和训练信号，不用精确方法顶替。
4. 退路 A（网络内部用精确刚度算残差）只在主线诊断之后作为单独一臂试，并如实标明。
5. 每个检查点自问：现在干活的是网络吗？

## 自主调整的边界
- 可以自己改：超参数、采样配比、架构细节（包括按流程块拆解和替换 v0 的部件）、诊断、实验顺序。
- 不自己改：第 2 节已定决定、验收门、把主线永久换成退路；不删数据、不加付费资源、不碰旧机器。
