# 今晚进展（2026-09-24 夜，持续更新）

计划见 `OVERNIGHT_PLAN_20260924_CN.md`。代码在 `docs/data/newmachine_20260924/src_s0/`。

## 第 0 步：基础设施（已完成）

### 几何与端口（fresh_train_0020 这一组）

| 几何 | 全部节点 | 盒面端口节点 | 切割带节点 | 端口自由度 | 内部自由度 |
|---|---|---|---|---|---|
| 重度切割 r1（保留 0.195，θ≈0.35°） | 22,408 | 1,246 | 5,148 | 18,858 | 48,366 |
| 中度切割 r2（保留 0.586，θ 38°） | 59,169 | 3,349 | 5,010 | 24,636 | 152,871 |
| FULL 母胞 | 96,498 | 5,836 | 0 | 17,508 | 271,986 |

切割带 = 带有正面积裁切面片的活动单元的全部 27 个节点（`fast_prep4.py`）。FULL 包里的“平面 x = 1”是盒面，已排除。

### 精确解（教师，`teacher.py`）

三个几何全部核验通过：刚体延拓误差 ≤ 7e-13；对称 2e-16；给力求位移的残差 ≤ 2e-12；与独立的稠密 Schur 补对照 ≤ 5e-15；灵敏度与有限差分对照 ≤ 2e-7。FULL 分解约 6 s，之后每个解约 10 ms。

### 晶格验收装置（`lattice3.py`）

- 切割胞加 FULL 母胞，x、y 两种构型；6 个盒面载荷计入验收门，3 个裁切面均布载荷（按平面上材料内积分点算一致节点力）只报告。
- 装置自检：被测算子换成精确算子时，柔度误差 1.7e-13（y 构型 2e-8），灵敏度误差 ≤ 1.4e-7。

### q 样本库（`prep_data.py`）

受力驱动方向确实最软：以单位能量归一后，Rayleigh 商（能量 / |q|²）的中位数，受力驱动约 1.8e-7（r2）到 3.9e-7（FULL），多项式约 1e-4，随机场约 2–3e-4，相差约 1000 倍。

### 零参数基线（不学习：活动网格上的图调和延拓）

晶格门（重度切割 r1）：

| 构型 | 体积加权：柔度 / 灵敏度 | 均匀：柔度 / 灵敏度 |
|---|---|---|
| x | 21% / 33% | 25% / 27% |
| y | 22% / 15% | 33% / 27% |

样本库上的能量误差（= 偏硬倍数 − 1，验证集均值）：

| 几何 | 受力驱动 | 多项式 | 随机场 | 最坏方向 |
|---|---|---|---|---|
| r1 | 8.4 | 0.32 | 0.37 | 78 |
| r2 | 14.4 | 0.45 | 0.47 | 72 |

结论：难点集中在受力驱动的软方向上；网络要补的是这部分。

### 一个机理上的发现

“弱节点”（3×3 对角块范数 < 中位数的 1%）约占 18–19%，不是零散的薄片，而是贴在壳两侧、连成片的虚拟区域节点：只靠极小的体刚度和 γ = 1e-4 的 GP 支撑。最软的模态在这里；端口里也有这类节点（FULL 924 个）。

## 架构

设计流程（四个方向 + 评审 + 综合）还在跑。已出的两份方案都把精确刚度、精确局部块求逆和粗层稠密 Cholesky 放进前向，网络只生成系数；按防跑偏规则，这不能作为主线。主线改为完全学习的多尺度线性 GNO（`models.py` 的 `MGNO`）：细层在活动网格上用几何生成的槽权重做单元超边消息传递，粗层 33³/17³/9³ 网格卷积（U-Net），对 q 严格线性，前向里没有任何精确分解；精确 K 只在能量损失和读出里。含精确刚度残差的版本以后作为对照臂（退路 A 类型）单独标明。

## 第 1 步：完全学习的 MGNO v0（重度切割 r1，只用能量损失）

- 单元测试：对 q 线性误差 1.3e-5（单精度），端口值精确，刚体能量 ~1e-15，读出对称 ≤ 1e-4；FULL 上每个 q 前向约 3 ms，含伴随的完整反力约 7 ms；参数 50 万。
- 3000 步短训练（3 分钟）过晶格门：x 构型柔度 8.0% / 灵敏度 17.9%，y 构型 16.2% / 15.6%（零参数基线 21–33%）。
- 2 万步长训练（r1，40 分钟）验证集能量误差（= 偏硬倍数 − 1）：

| 步数 | 受力驱动 | 多项式 | 随机场 | 最坏方向 |
|---|---|---|---|---|
| 2000 | 1.81 | 0.069 | 0.098 | 26 |
| 10000 | 0.12 | 0.008 | 0.033 | 2.3 |
| 20000 | **0.023** | **0.004** | **0.020** | **1.54** |
| 零参数基线 | 8.4 | 0.32 | 0.37 | 78 |

  晶格门测量排在队列里（首次因 GPU 被并行训练占满而爆显存，已改为稠密晶格分解放在 CPU 上、稠密端口算子缓存到盘）。
- r2（中度切割）2 万步训练中：第 12000 步受力驱动 0.10、最坏方向 2.5。

### 误差分块诊断（r1，2 万步，受力驱动方向，总能量误差 0.024）

- 36% 的误差能量在 GP 项里：网络在虚拟边缘节点上的场跨面跳变不对。
- 体误差中 45% 在含弱节点的单元，33% 在贴端口的单元，22% 在其余内部单元。
- 结论：软方向上约三分之二的误差来自虚拟边缘区，这是细层局部部件的职责。据此做了 MGNO2（仍然完全学习）：每层细层后加 GP 面超边层（27 槽：owner 靠面两层 + 邻居中间层），末尾加 4 对只作用于边缘单元 / 面的层；用梯度检查点控制显存。单元测试通过（线性、端口、刚体、伴随对称）。

### 与四份设计方案的关系

四份方案（多层级、算子学习、力学、系统）都主张前向里放约 10 次精确刚度作用、精确局部块求逆和粗层稠密求解，只学门控系数，理由是纯学习前馈网络要做到 1e-4 的抵消精度。评审指出，这类方案按收缩理论实际需要 12–24 个循环才能过门（与路线 1 的历史一致），也就是退路 A/B。实测表明完全学习的前馈网络 20 分钟内就把软方向能量误差从 8.4 压到 0.02，所以主线保持完全学习；精确部件只作诊断或标明的对照臂。

### 灵敏度监督

新增辅助损失：样本库每个 q 的 8 个角点精确灵敏度（教师算，`prep_sens.py`），训练时加相对误差项。验收用的晶格工况不进训练，没有泄漏。r1 上“能量 + 灵敏度”一臂排在队列中。

## 晶格门：MGNO v0（r1 重度切割，2 万步，只用能量损失）

注意：这一版只算被测切割胞的灵敏度，邻居 FULL 胞的灵敏度跳过（GPU 被并行训练占满，算邻居精确场的分解放不下）；训练结束后补全。柔度不受影响。

| 构型 | 柔度误差 | 被测胞灵敏度误差 | 裁切面受载：柔度 / 灵敏度 | 对照：零参数基线 柔度 / 灵敏度 |
|---|---|---|---|---|
| x | **2.1%** | 5.6% | 1.1% / 7.5% | 21% / 33% |
| y | 5.1% | 4.7% | 5.0% / 1.3% | 22% / 15% |

- 柔度 x 构型已过 3% 门，y 构型 5.1%；灵敏度 4.7–5.6%，未过门。离门都在 2 倍以内。
- 晶格方向的误差接近样本库受力驱动方向的最大值那一端（验证集受力驱动均值 2.3%、最大 4.8%），说明样本库里“自由漂浮的胞受力”的方向覆盖不够像晶格里“一面贴着邻居”的状态。据此新增“部分支撑”样本类：一个盒面加弹簧（刚度为该面对角刚度均值的 0.01–1 倍），其余端口施力，教师精确求解（`prep_support.py`，不用任何验收工况）。

## 正在跑的对照（都在 r1 上）

- 能量 + 灵敏度监督（MGNO v0）。
- MGNO2（GP 面超边 + 边缘层），只用能量。第 4000 步受力驱动 0.85，同一步 v0 为 2.0。
- 组合臂 r1_d（排队中）：MGNO2 + 灵敏度监督 + 部分支撑样本，3 万步。

工程上的修正（今晚踩过的坑）：
- 教师 Cell 的显存从约 7 GB 降到 3.1 GB（FULL）：GP 矩阵改在 CPU 上组装并缓存（与 GPU 版逐项一致，差 4e-16），刚度只存上三角 CSR 加转置、int32 索引；精确内部场改用单精度分解 + 双精度修正（与双精度解差 7e-15）。
- 稠密端口算子缓存到盘；稠密晶格分解可放 CPU。
- 队列：评估等所有训练结束后再跑并加锁，新训练给评估让路。

## 晶格门汇总（只算被测胞灵敏度；邻居灵敏度待补）

| 几何 | 模型 / 训练 | x：柔度 / 灵敏度 | y：柔度 / 灵敏度 | 裁切面受载 x / y（柔度 / 灵敏度） |
|---|---|---|---|---|
| r1 重度切割 | 零参数基线 | 21% / 33% | 22% / 15% | 17% / 46%；33% / 16% |
| r1 | MGNO v0，3000 步 | 8.0% / 17.9% | 16.2% / 15.6% | 3.9% / 7.6%；16% / 16% |
| r1 | MGNO v0，2 万步，能量 | **2.1%** / 5.6% | 5.1% / 4.7% | 1.1% / 7.5%；5.0% / 1.3% |
| r1 | MGNO v0，2 万步，能量 + 灵敏度 | **2.0%** / 3.4% | 6.2% / 5.0% | 1.1% / 5.5%；4.3% / 1.5% |
| r2 中度切割 | 零参数基线 | 57% / 89% | 58% / 89% | 30% / 89%；46% / 91% |
| r2 | MGNO v0，2 万步，能量 | 3.2% / 10.1% | **2.9%** / 10.0% | 1.7% / 4.2%；3.0% / 4.2% |

观察：
- 完全学习的网络把晶格柔度误差从 21–58% 压到 2–6%，r1 x 构型和 r2 y 构型已过 3% 门；灵敏度 3.4–10%，都还没过。
- 灵敏度监督让 r1 的 x 构型灵敏度从 5.6% 降到 3.4%，但 y 构型没有改善（柔度 5.1% → 6.2%）。y 构型是邻居贴在 y = 0 面、载荷加在 x = 0 面；训练样本里缺少“一面被支撑”的状态，这正是新加的“部分支撑”样本类要补的。
- 验证集上的样本库误差已经很小（r1 受力驱动方向能量误差 2.2%、灵敏度误差 0.7%），晶格门却在 2–6%：误差集中在晶格实际出现、而样本库覆盖不足的方向上。下一步的重点是训练方向的分布，而不是容量。

排队中：r1_e（v0 + 灵敏度 + 部分支撑，3 万步）、r2/FULL 的灵敏度标签、FULL 训练（v0 + 灵敏度）。

## 09-24 上午（用户决定后）

### 验收门改用按面力一致的载荷

- 设计文档第 7 节已改：计入门的是按面力一致的盒面载荷；端口节点均布节点力作为压力测试照报。灵敏度门对晶格中每个胞、每个计入门的载荷取 8 个角点向量的相对误差。
- `evalnet.py` 默认先算 consistent（role = gate），再算 uniform（role = stress）。
- 队列修正：评估之间互斥（mkdir 锁），避免两个评估同时分解导致 cuDSS 显存分配失败（r2 完整门曾因此失败一次，已重排）。

### 新门下的完整判决（灵敏度含邻居胞）

| 几何 | 模型 | 构型 | 柔度 | 被测胞灵敏度 | 邻居胞灵敏度 | 过门 |
|---|---|---|---|---|---|---|
| r1 | MGNO v0 + 灵敏度 + 部分支撑（3 万步） | x | 2.11% | 2.53% | 1.47% | 是 |
| r1 | 同上 | y | 1.22% | 2.10% | 0.59% | 是 |
| FULL | MGNO v0 + 灵敏度（2 万步） | x | 0.67% | 2.01% | 0.09% | 是 |
| FULL | 同上 | y | 0.68% | 1.51% | 0.05% | 是 |
| r2 | MGNO v0 + 灵敏度 + 部分支撑（2 万步） | x | 1.70% | 4.04% | 1.13% | 否 |
| r2 | 同上 | y | 1.52% | 4.83% | 0.51% | 否 |
| r2 | MGNO2 + 灵敏度 + 部分支撑（2 万步） | x | 1.85% | 2.88% | 算中 | 被测胞过 |
| r2 | 同上 | y | 1.58% | 4.42% | 算中 | 否 |

压力测试（均布节点力）：r1 x 0.89% / 2.46%，y 1.22% / 2.10%（过）；FULL 2.3–2.4% / 8.2–8.5%；r2 v0 2.2–2.5% / 9.3–10.3%，MGNO2 1.9–2.1% / 6.5–6.7%。

### r2 y 构型的超门项

MGNO2 在 y 构型只有 nbr_facex0_z（邻居胞 x = 0 面的 z 向载荷）超门：被测胞灵敏度 4.4%，被测胞应变能占比 0.1%。v0 也是同一个载荷最差（4.8%）。这一类工况里被测胞自己不受载，被邻居经贴合面拖动，贴合面上的力自平衡。

- 新样本类 face（`prep_face.py`）：力只在一个盒面上（平面波 75% / 高斯斑块 25%），投影到该面的自平衡子空间（面上刚体模态正交），其余端口自由，q = S⁺f，单位精确能量，附精确灵敏度。
- 对照臂：d = 从 mgno2_r2_c 检查点继续 1 万步，采样加 face 20%；e = 同样继续 1 万步，不加 face。两臂训练后都跑新门。
- `train1.py` 增加 `init`（从检查点热启动）和 `pct_start`。

### 提速一：单元矩对 τ 的导数（`moments_ad.py`）

- 做法：与 `polyref_torch_fast.cell_moments` 同样的几何和算术，改成每个单元带自己的 τ 和切割面、结果保留为可求导的张量；裁切点是 ψ 值的线性插值，ψ 对 τ 线性，所以固定裁切拓扑下可以直接反向求导。积分点那一段用检查点（反向时重算），显存从 13 GB 以上降到约 4 GB。
- 设计灵敏度 s = −∂/∂τ Σ M(τ)·g，g 为能量密度；多个载荷的加权和只需一次反向。
- 对照（固定场，4 个白噪声场 + 4 个三次多项式场）：

| 几何 | 单元数 | 与有限差分的相对差（步长 1e-3 / 1e-4 / 1e-5） | 有限差分 16 次求值 | 反向求导（加权总灵敏度） |
|---|---|---|---|---|
| r1 | 2244 | 6.0e-7 / 4.7e-9 / 4.0e-10 | 3.8 s | 1.1 s |
| r2 | 6007 | 2.3e-7 / 1.7e-9 / 4.9e-10 | 10.0 s | 2.9 s |
| FULL | 9859 | 3.0e-7 / 1.1e-9 / 8.0e-10 | 16.0 s | 4.9 s |

  差值随步长按平方下降，说明反向求导给的是精确导数（有限差分自身有截断误差）。单元矩本身与原程序逐行差 ≤ 2e-12（求和顺序不同）。三个胞合成一次调用（18110 个单元，各带自己的 τ 和切割面）与逐胞调用结果一致，但不更快：计算量在单元本身。

### 提速二：网络推理（`fastnet.py`）

- 几何路径只算一次；每个超边层预拼成两个稀疏矩阵（收集 G：H·E × N，值为 α；散射 S：N × H·E，值为 β / 度），网格限制和延拓（含跳连系数）也是稀疏矩阵；反力 Ŝq = ÊᵀKÊq 用显式转置（转置稀疏矩阵、通道混合转置、conv_transpose3d、层序反转），不建自动求导图。仍是单精度，K 的乘法用双精度，和训练时一致。
- 对照（`t_fast.py`）：

| | r1，MGNO v0 | r2，MGNO2 |
|---|---|---|
| 能量 qᵀŜq 相对差 | 7.1e-6 | 4.7e-6 |
| 伴随恒等式 ⟨Êq, v⟩ = ⟨q, Êᵀv⟩ | 3.6e-6 | 4.3e-7 |
| 位移场相对差（含白噪声输入） | 1.9e-4 | 4.3e-5 |
| 反力 Ŝq，1 个方向：时间 / 额外显存 | 13.0 → 3.8 ms；0.45 → 0.03 GB | 53.6 → 9.5 ms；2.0 → 0.06 GB |
| 反力 Ŝq，9 个方向 | 22.7 → 13.8 ms；2.2 → 0.31 GB | 130 → 57 ms；5.7 → 0.54 GB |
| 反力 Ŝq，16 个方向 | 32.6 → 22.6 ms；3.7 → 0.55 GB | 旧路径显存溢出 → 98 ms，0.95 GB |
| 反力 Ŝq，64 个方向（仅新路径） | 87 ms，2.2 GB | 366 ms，3.8 GB |

- 位移场 1e-4 量级的差是这个网络单精度求值本身的舍入水平：以双精度重算原网络为参照，两条单精度路径的误差一样大（r1：银行方向 0.9–2.7e-4，白噪声输入 3.9e-4 对 3.8–4.1e-4，转置 4.2e-4 对 4.3e-4；r2：0.2–1.0e-4 两者相同）。
- 端到端：用快速网络重算 r1 晶格门（按面力一致载荷，含邻居灵敏度），与原结果一致，PCG 迭代次数相同：

| 构型 | 柔度（快速 / 原） | 灵敏度（快速 / 原） | PCG |
|---|---|---|---|
| x | 2.1088% / 2.1085% | 2.5247% / 2.5254% | 12 / 12 |
| y | 1.2222% / 1.2222% | 2.0977% / 2.0969% | 14 / 14 |

  结论：快速版本与原网络是同一个算子（差在单精度舍入量级），评估脚本加了 `FASTNET=1` 开关。训练仍用原模型代码。


### r2 晶格误差拆分（`diag_lat.py`，MGNO2 r2，按面力一致载荷）

对每个载荷分别算：总误差（门测的，网络场 + 网络晶格解）、只有延拓误差（网络场 + 精确晶格解的端口位移）、只有界面误差（精确场 + 网络晶格解的端口位移），都相对精确灵敏度。

| 构型 | 载荷 | 总误差 | 只有延拓误差 | 只有界面误差 | 端口位移相对误差 | 被测胞能量占比 |
|---|---|---|---|---|---|---|
| y | test x / y / z | 2.6 / 2.4 / 2.3% | 7.5 / 6.4 / 5.3% | 7.8 / 7.2 / 6.6% | 1.2 / 1.9 / 0.9% | 18 / 41 / 13% |
| y | nbr x / y / z | 1.9 / 2.0 / **4.4%** | 5.6 / 3.1 / 1.0% | 3.3 / 4.0 / 4.8% | 0.21 / 0.09 / 0.04% | 0.75 / 0.34 / 0.13% |
| x | test x / y / z | 2.9 / 2.7 / 2.4% | 8.1 / 9.9 / 6.6% | 8.6 / 9.3 / 7.4% | 2.0 / 1.4 / 0.9% | 39 / 22 / 13% |
| x | nbr x / y / z | 1.0 / 1.5 / 2.7% | 4.4 / 6.6 / 2.4% | 3.9 / 4.7 / 3.9% | 0.12 / 0.30 / 0.04% | 0.44 / 1.1 / 0.13% |

- 两个误差来源单独都有 5–10%，但方向相反、在总误差里大部分抵消：网络偏刚（Ŝ ≥ S），晶格解的端口位移偏小，用精确场算的灵敏度偏低；网络的延拓场能量偏高，在精确端口位移上算的灵敏度偏高。网络自己的场和自己的反力是一对自洽的变分近似，所以门测的总误差只有 2–3%。
- 超门的 nbr z（y 构型）几乎全是界面误差（4.8%，延拓误差只有 1.0%）：被测胞基本被整体拖着走，端口位移的相对误差只有 0.04%，但它的变形部分很小，这点误差相对变形部分就大了。要改善的是这类方向上 Ŝ 的精度，正是 face 类的能量损失所针对的。
- 旁证：未见过 face 类的 MGNO2 在 face 方向上的银行误差（能量 1.8%，灵敏度 1.3%，p90 2.8%）并不比受力驱动类差；晶格里的这个方向落在尾部。

### 灵敏度重排（`teacher.Cell.sens2`）

s_ck = −Σ_e u_ekᵀ A_ce u_ek，A_ce = Σ_m dM_cem Tm_m 按单元块现算：每个场 8 次缩并代替 125 次。与原写法差 3e-14 / 5e-14，r1 0.60 → 0.046 s、FULL 2.65 → 0.20 s（128 个场）。只用于数据准备（`prep_geo.py`）；训练里的灵敏度损失没有改。

### 第 2 步数据（用户 09-24 同意：先把现成的 209 个几何准备好）

- 试点（fresh_train_0031 族 5 个几何，五类样本各 512 / 64 / 64，附精确灵敏度）：每个几何 70–205 s，平均 140 s，约 270 MB，全部通过。
- 提速后（灵敏度重排，部分支撑每面一个弹簧档）正式跑 209 个：CPU 阶段（建模 + GP 缓存）与 GPU 阶段（`prep_geo.py`）流水线进行；GPU 阶段每个几何取一次评估锁、先查显存，出错写 FAILED.json 跳过，可断点续跑。输出在 `/root/autodl-tmp/OPL/S2/data`，第 1 步的数据不动。
- 对照臂 e（不加 face 的控制组）在与 d 臂同时训练时显存不足退出，已排到数据准备完成之后重跑。

- 显存问题与处理（数据准备与 d 臂训练共用一块 32 GB 显卡，d 臂占 17.8 GB）：
  1. 最初同时保留内部和 Neumann 两个双精度分解，中等以上几何放不下 → 改为单精度分解（内部分解在延拓里做 3 次双精度迭代修正，与双精度解差 7e-15；Neumann 和弹簧解只用来生成采样方向，方向随后用精确能量归一），且任何时刻只保留一个分解（Neumann → 弹簧逐个 → 内部）。
  2. cuDSS 分配失败后，已申请的显存不会释放，进程里累积 → 改为每个几何一个进程。
  3. 大几何（约 30 万自由度）在 d 臂训练期间放不下 Neumann 分解 → 第一遍跳过、记失败；第一遍结束后第二遍自动重试这类几何，要求空闲显存 ≥ 22 GB（d 臂那时已结束）。控制臂 e 等全部完成的标记再开训。

### 零样本基线：只在一个几何上训练的网络用到别的几何上（`zs_eval.py`，快速网络，验证集样本库）

| 训练几何 → 测试几何 | 受力驱动能量误差 均值 / 最大 | 部分支撑 | 单面受力 | 宏观 | 随机场 | 灵敏度误差（受力驱动） |
|---|---|---|---|---|---|---|
| 0020 r2（MGNO2）→ 0031 中度切割 | 306% / 961% | 353% | 66% | 85% | 41% | 83% |
| 0020 r2（MGNO2）→ 0031 重度切割 | 107% / 204% | 95% | 21% | 7% | 6% | 47% |
| 0020 r2（MGNO2）→ dev_0000 重度切割 | 407% / 2151% | 254% | 79% | 14% | 9% | 131% |
| 0020 FULL（MGNO v0）→ 0031 FULL | **8.5% / 17%** | 16% | 7% | 2.6% | 3.0% | 12% |
| 0020 FULL（MGNO v0）→ dev_0002 FULL | 2236% / 6441% | 9255% | 1148% | 1745% | 869% | 219% |
| 0020 r1（MGNO v0）→ dev_0000 / 0031 重度切割 | 1.2e3–2.8e3 倍 | — | — | — | — | 180% |

（对照：各网络在自己训练的几何上，受力驱动方向误差 1.6–2.4%。）

- 第 1 步的网络是逐个几何拟合出来的，不能直接用于新几何；只有几何很接近时（两个厚度场相近的 FULL 母胞）才有部分迁移（8.5%）。
- 几何差别大时误差失控到几十到上千倍：网络在新几何上生成的场在某些区域（多半是虚拟边缘区）带出巨大的多余能量。
- 这是第 2 步的起点：泛化必须靠多几何训练学出来，目标是把百分之几百到上千压到几个百分点。
- 另：d 臂（热启动最高学习率 1e-3）第 2000 / 4000 步验证误差明显变差（受力驱动 10% / 8%，起点 2.4%；最坏方向 1.7 / 2.2），判断是学习率过高把收敛的模型推开，已停掉，改为最高学习率 3e-4 重跑（d2），控制臂 e 同样改为 3e-4。
- 数据准备中遇到一个几何的内部分解在单精度下数值失败（cuDSS INVALID_VALUE，条件数太大），已加自动回退到双精度。dev_0003 这一族的几何有 40–43 万自由度，都放到第二遍。

### r2 d2 臂（MGNO2 从 mgno2_r2_c 热启动 1 万步，最高学习率 3e-4，采样加 face 20%）

样本库（验证集）最终：受力驱动 1.83%（起点 2.36%）、部分支撑 1.74%（2.23%）、face 0.98%（1.77%）、宏观 0.59%（0.73%）、随机场 2.07%（2.56%）；灵敏度 0.6–1.5%；最坏方向 1.31（1.48）。

晶格门（按面力一致载荷，含邻居胞灵敏度）：

| 构型 | 柔度 | 被测胞灵敏度（最大） | 邻居胞灵敏度（最大） | 过门 |
|---|---|---|---|---|
| x | 1.50%（原 1.85%） | 2.58%（原 2.88%） | 1.06% | 是 |
| y | 1.29%（原 1.58%） | **3.10%**（原 4.42%） | 0.60% | 否，差 0.1 个百分点 |

- y 构型除 nbr_facex0_z（被测胞能量占比 0.13%）外，所有载荷的被测胞灵敏度 ≤ 2.2%；这一项从 4.42% 降到 3.10%。x 构型同一类载荷从 2.69% 降到 1.60%。
- 裁切面受载（只报告）：x 1.2% / 2.7%，y 2.1% / 2.6%。压力测试（均布节点力）x：1.62% / 5.25%（原 6.7%）。
- 改善来自 face 类还是来自多训练的 1 万步，要等控制臂 e（同样的热启动和学习率、不加 face）结果；e 臂排在第 2 步数据准备完成之后。

## 09-24 晚：第 2 步数据完成、训练加速

### 数据
- 209 个几何中 206 个完成，3 个是没有内部自由度的退化薄片（所有节点都是端口，延拓为恒等），排除。共 58 GB，约 5 小时（与 r2 训练共用显卡；独占时估计 3.5–4 小时）。每个几何中位 70 s。
- 划分（按族，`make_split.py`）：训练 148 个几何（28 族）、验证 20（4 族）、测试 38（8 族，即 development）。
- 用户决定：几何覆盖只考虑体积分数 0.1–0.4（已由厚度映射保证）、裁切角 0–45° 单方向（其余靠旋转增广）、深度全程；三维斜切和多刀切列为后续工作。不跑 50/100/150 学习曲线，改为全部 148 个几何训一次，用训练几何与留出几何的误差差距判断数据是否够。

### 训练步剖析（r2，MGNO2，批量 16，显卡独占 0.41 s/步）
网络前向 + 反向 88%（前向只有 0.08 s），灵敏度损失 10%，能量（双精度刚度乘法）2%，对抗与验证摊到每步约 3%。

### 加速（每项都在同一批数据上核对损失和全部参数梯度）
| 改动 | 性质 | 核对 | 效果 |
|---|---|---|---|
| 超边层改稀疏矩阵 + 手写反向，去掉检查点（`sparse_layers.py`） | 数学等价 | 损失差 4e-6；梯度差 0.44%，与原写法自身两次运行的差（0.31–0.59%，原子累加顺序）同量级 | 与下两项合计 |
| 槽位权重梯度改用 cuSPARSE SDDMM | 数学等价 | 同上 | 反向中原来 42% 的批量小矩阵乘法消失 |
| W 的梯度（长 20 万的窄矩阵乘法）分段求和 | 数学等价 | 同上 | 每步省约 75 ms |
| MGNO2 几何路径去掉重复的嵌入计算 | 数学等价 | 同上 | 约 10% |
| 灵敏度损失重排（8 次代替 125 次缩并） | 数学等价 | 值差 1e-4，梯度差 1e-5 | 0.043 → 0.018 s |
| 几何预构建存盘（`prebuild.py`） | 不涉及计算 | — | 省去每个几何 10–20 s 的首次构建 |
| 从 r2 的 MGNO2（d2）热启动 | 改变训练路径 | 正式训练中看 | 预计显著减少步数 |

合计：每步 0.38 → 约 0.15 s（与其他任务共用显卡时实测 0.56 → 0.21 s，2.7 倍）。

试过但不用：cudnn.benchmark（无收益）、torch.compile 几何路径（反而变慢 0.20 → 0.37 s）、一步两个几何各 8 个方向（每样本成本 1.7 倍）。有损、不做：半精度 / bf16 / TF32（网络单精度本身已有约 1e-4 舍入，损失又把梯度噪声放大约 50 倍）。

### 第 2 步正式训练（排队中，预构建完成即开始）
MGNO2（稀疏层）、148 个训练几何、显卡常驻 6 个几何每 300 步换一个（换入时算最坏方向）、6 万步、批量 16、从 d2 热启动；每 1 万步在 20 个验证几何和 12 个训练几何（没见过的 q）上评估，看泛化差距。预计约 3.5 小时。

### 第 2 步正式训练：第一次在 1150 步中断，已修复并重启（19:37）
- 现象：进程在第 1150 步后消失，日志没有报错（19:04）。容器 cgroup 没有内存超限或 OOM 记录，确切死因无法确认；可能是进程挂在 Jupyter 内核的进程组下被连带结束。
- 排查中查到并修复了三个真问题（每个都会让 6 万步的训练跑不完或跑坏）：
  1. 内存只增不减：换出的几何和 20 个验证几何全部留在主机内存（到 1150 步已加载 26 个、59 GB，容器上限 90 GiB）；而且 GPU→CPU 的 `non_blocking` 拷贝落进 PyTorch 永不释放的锁页缓存（大小按 2 的幂取整）。冒烟测试里约 15 次换入就把常驻内存从 3 GB 推到 28 GB。改为：换出的几何直接释放、需要时从磁盘缓存重读（约 2–5 s）；验证逐个加载、评完即放；拷贝改为阻塞。修后同样的测试常驻内存在 2–16 GB 之间波动、不增长。
  2. 显存：池里同时有两个 40 万自由度的几何时（按当前顺序在第 1200 步就会出现）超出 31.4 GB。加了池的自由度上限（含换入者合计 ≤ 70 万，超出时最老的提前离开）。按实际顺序模拟 6 万步，大、小几何平均分到的训练步数与不设上限时相同（411/397 对 422/395）。
  3. 5 个几何（训练 3 个：0027_d0_v0、0022_cover01_r2、0008_cover01_r1；验证 1 个：0003_d0_v0；测试 1 个：development_0007_cover01_r2）的 support 类约 89% 的样本是 NaN：单精度弹簧分解在重度裁切、某些面几乎空的几何上悄无声息地给出 NaN（只有抛异常才退回双精度）。训练采样不滤 NaN，一旦抽到就会让整个网络参数变成 NaN。现在加载时剔除非有限样本（这 5 个几何各剩 58/8/8 个 support 样本，其他类不受影响），训练循环再加一道非有限损失 / 梯度保护（跳过该步并记录）。以后用双精度重算这 5 个几何的 support 数据。
- 其他加固：每 2000 步存断点（模型 + 优化器 + 学习率调度），`resume` 可续跑（冒烟测试中续跑与不中断的结果一致）；用 `setsid` 脱离内核进程组，外加守护脚本（意外退出且无 DONE 时自动从断点续跑，最多 5 次）；监控加了“进程消失”报警。
- 重启配置：池 3 个 + 自由度上限 70 万，其余不变（6 万步、批量 16、每 300 步换入、每 1 万步评估 20 个验证几何和 12 个训练几何）。

### 第 2 步第 1 万步验证与第二轮架构评审（20:35–20:45）
- **第 1 万步：**
  - 留出几何：force 0.425、support 0.553、face 0.162、macro 0.058、grf 0.061；
  - 训练探针：0.460、0.540、0.108、0.050、0.053；
  - 比第 1 步单几何差 10–25 倍；
  - 薄、小几何最差（力类 1.1–1.6），FULL 母胞和大几何 0.04–0.12；
  - 池内损失 3k–10k 步停在平台上；
  - 探针实际测的是遗忘。
- **第二轮架构评审**（五视角 + 两位评审 + 综合）已完成，见 `docs/ARCH_REVIEW_20260924_CN.md`：
  - 结论是主干不换；
  - 先做诊断，再按判据做只动几何路径的加固和晶格情境采样。
- **A0：**快照 `snap_10000.pt` 已存，之后每次验证自动存快照。
- **A1（CPU）：**
  - median(d3) 跨几何差 9.9 倍 → B2 触发；
  - 矩比从不超出 [−1, 1]；
  - 无划分泄漏。

### 第 2 步 v1 完成（09-25 00:13，6 万步，4.6 小时，中途无重启）

各次验证的类均值（能量超出量；左边 20 个留出几何，右边 12 个训练几何、没见过的 q）：

| 步 | force | support | face | macro | grf |
|---|---|---|---|---|---|
| 10k | 0.425 / 0.460 | 0.553 / 0.540 | 0.162 / 0.108 | 0.058 / 0.050 | 0.061 / 0.053 |
| 20k | 0.152 / 0.159 | 0.186 / 0.168 | 0.101 / 0.070 | 0.022 / 0.017 | 0.036 / 0.029 |
| 30k | 0.296 / 0.271 | 0.363 / 0.297 | 0.119 / 0.070 | 0.028 / 0.021 | 0.036 / 0.028 |
| 40k | 0.094 / 0.093 | 0.109 / 0.096 | 0.057 / 0.035 | 0.012 / 0.009 | 0.020 / 0.017 |
| 50k | 0.071 / 0.052 | 0.084 / 0.054 | 0.056 / 0.017 | 0.011 / 0.008 | 0.019 / 0.015 |
| 60k | **0.052 / 0.039** | **0.062 / 0.041** | **0.033 / 0.015** | **0.010 / 0.008** | **0.020 / 0.016** |

**参考**：第 1 步单几何网络在自己的几何上是 force 0.018–0.024、support 0.017–0.022、face 0.010–0.018、macro 0.006、grf 0.021–0.026。

**读法：**

- **30k 的回退。**集中在薄的、重度切割的几何上（0029_cover01_r2 的 force 从 0.22 升到 0.74）。原因是那段时间池里都是厚几何，学习率还在 5–7e-4，网络发生了按几何类型的遗忘，随后学习率下降又恢复。训练器 v2 的快速换入、定额、EMA、低学习率就是针对这一点。
- **留出几何与训练几何的差距到后期才出现**：60k 时 force 1.34 倍、support 1.53 倍、face 2.2 倍。
- **完整胞**（FULL 母胞，val 与训练集都算）force 只有 0.009–0.011，已经优于单几何网络。
- **薄、切割几何**在 0.03–0.09。最差的是 0020_cover01_r1（第 1 步的 r1 重度切割，这里作为留出几何）：force 0.226、face 0.211。
- **朝向敏感度**（t_oh_remote，几何 0021_cover01_r1，它和它的 5 个旋转包都在训练集）：48 个朝向下 force 误差，原朝向 0.070、平均 0.398、最差 0.888；support 0.072 / 0.338 / 0.682；macro 和 grf 只放大约 2 倍。同一个胞换一个朝向放进晶格，力类误差平均会大 5 倍以上，所以 O_h 增广必须做。
- **O_h 视图的正确性**：在 5 个真实旋转包上，场差 4.5–5.4e-5，能量差 3.8–8.3e-5，这是 fp32 网络本身的舍入量级；错误的变换会差到约 0.5。
