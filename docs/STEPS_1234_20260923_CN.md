# 按 Codex 复核推进的四步（2026-09-23，进行中）

顺序与用户确认一致：1 双侧全谱 → 2 端到端成本实测 → 3 设计灵敏度 → 4 用旧 E1/E2 做困难方向归因。
源码副本在 `docs/data/takeover_20260923/src_step1234/`，服务器目录见各节。

**验收门变更（用户已同意）**：主验收门改为“晶格装配后的柔度和灵敏度，误差在 3% 以内”；单胞全谱门降为诊断。
下面第 1 步的全谱结果只作诊断用，不再是通过条件。第 2 步已取消（见该节）。

## 1. 双侧全谱（`FULLSPEC_DIRECT_01/`，进行中）

做法与冻结的全谱协议相同：G[:,j] = R^-T B S B^T R^-1 e_j，所有 d 个物理方向都保留，对称化后算全部特征值，
D/d = mean(μ − 1 − ln μ)。区别只在 S 用 cuDSS 精确作用（单元是多面体积分，有效分辨率 8）。

校验：中度 train13 用这条路径得到 [0.99923, 1.0064]、D/d 1.47e-7，与之前独立的 PARDISO 精确凝聚结果
[0.99923, 1.0064]、1.5e-7 一致。

已完成 7 个第二批样本（其余在跑）：全部通过目标门，最低 μ 0.99630，最高 1.00791，D/d 最大 2.4e-7。

## 2. 端到端成本（`E2E_01/`，已取消）

原计划：单个样本、单进程顺序执行 G 阶段 → P 和 GP → GPU 编码，记录墙钟、峰值内存和显存。

2026-09-23 06:22 UTC 经用户同意取消。排队进程在任何样本开始前就已停止，`E2E_01/status` 里有记录。
取消的原因是验收门已经改了，方向也在重新讨论，单胞计时不再影响决策。
已有的分段计时仍可作参考：GPU 编码 1–18 秒，不含 P 和 GP；按 Codex 的加总，含 P 和 GP 时中位约 75 秒，最大约 267 秒。

## 3. 设计灵敏度（`SENS_01/`，进行中）

沿用 2026-09-19/20 的 τ 门协议：8 个角点同乘精确有理数 (1+ε)，ε ∈ {±1e-4, ±1e-3}。
- 教师侧：私有面板上重算 G 阶段 → 冻结代码重建 P、GP → 精确单元组装 A/C/D。
  只有 P、GP、刚体子空间都与基准完全相同的扰动才算可比（否则是单元出生/死亡，算子不在同一基上）。
- 几何侧：同一 P、GP，只把多面体积分的 τ 换成 τ(1+ε)。
- 观测量（两侧同一段代码）：非刚体子空间上的 log det、trace(S^-1)、最软/最硬特征值、8 个固定载荷的柔度。
- 比较对数斜率 d ln(obs)/dε，门槛 3%。

样本 0008、0020、0013。

**准入判据的一个错误（已修正，结果重算中）**：第一版把重建的 GP 矩阵和基准的 GP 矩阵逐元素比较，要求完全相等。
但两边的构造写法不同（一个向量化，一个循环），浮点求和顺序不同，最大相对差 1.2e-18，于是 12 个扰动全部被判为不可比。
核对后确认，0008 在 ε = −1e-4 时的 GP 面列表与基准完全相同，节点集合也相同。
重建矩阵和基准矩阵之间只差舍入误差。

第二版改为比较 GP 面列表本身，也就是整数，要求完全相等。面列表的基准是数据包里发布的 GP_FACES，基准样本当初已经核对过与它一致。
另外要求节点集合相同、P 完全相同、刚体子空间残差不超过 1e-12。三个基准点也走同一段代码重算，不再从 COVER_INPUTS 复制，这样五个点用的是同一种构造。
源码：服务器 `xcase_src_24/`，仓库 `docs/data/takeover_20260923/src_step1234/`（sens_blocks.py 第二版、blocks24_one.sh、run_blocks24.sh、run_sens_obs24.sh、ghost_diag.py）。

按第一版已经记下的维数和 P 对比，预计的准入情况如下：
- ε = ±1e-4：三个样本全部可比。
- ε = −1e-3：三个样本的边界维数都变小了：0008 从 14304 降到 14292，0013 从 17346 降到 17298，0020 从 13578 降到 13566。
  其中 0008 和 0020 少了活动单元，0013 的活动单元数没变，是切割结构变了。
- ε = +1e-3：0020 的维数没变，但迹坐标 P 变了，GP 面数也从 4594 变成 4589。

也就是说，τ 相对变化 1e-3 时，6 个方向里有 4 个的离散拓扑已经变了。
离散 CutFEM 模型的柔度对 τ 只是分段光滑，灵敏度门必须在同一拓扑内比较。正式数字待重算完成后补上。

## 4. 旧网络 E1 在困难方向上是否也出错（`ATTR_01/`）

中度 train13（E1 的训练样本之一），E1 = M_q 标签、6 万步。坐标系与我们的见证完全一致（Householder 帧差 1e-16）。

| 方向 | E1 柔度比（教师 = 1） | E1 刚度比 |
|---|---|---|
| 32 个随机方向 | 1.68 – 1.81（中位 1.74，整体偏软） | 15.8 – 985（中位 196） |
| 求解器困难方向（原骨架 64 层，μ = 12.6 的方向） | **0.377（偏硬 2.65 倍）** | 66.2 |
| 几何单元误差最大的方向 | 3.20 | 1.6e4 |
| E1 自己的两个极端特征方向 | 65.1 / 3.2e-7 | 0.0154 / 3.1e6 |

- 困难方向对 E1 确实特殊：E1 在其他方向整体偏软，只有在这里偏硬，而且落在随机方向的范围之外。
- 但它不是 E1 最差的地方：E1 自己的极端特征方向与困难方向正交（余弦约 0），前 24 个边界坐标完全不重叠。
  E1 的误差范围跨 8 个数量级（μ 从 0.015 到 3.1e6），绝大部分不在强耦合节点对上。
- 所以强耦合节点对只能解释 E1 失败的一小部分。只有一个样本，而且是训练样本；E2 等全谱批次跑完后再算（内存需求大）。
