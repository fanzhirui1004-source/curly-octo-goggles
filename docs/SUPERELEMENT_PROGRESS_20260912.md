# 两尺度超单元：V0 与 V1 进展记录

2026-09-12。对应设计文档 `NEURAL_REDESIGN_20260911.md` 的第 7 节验证阶梯。代码在 `superelement/`，服务器副本在 `/root/cutfem_neural_a_20260910/superelement_v0/`，运行产出在 `/root/autodl-tmp/CUTFEM_SUPERELEMENT_20260912_*`。

## V0：自由系数表示容量测试（0415）

最终形式：Ŝ = L_GGᵀ (I + MMᵀ)⁻¹ L_GG。L_GG 是面网格上半径 0.2 的带状上三角因子，对角用教师对角初始化；M 是 512 个稠密全局软化列；严格正定，无约束。损失是教师能量度量的相对误差，探针为白化均匀方向、粗空间光滑场、支撑加权细场，外加各向同性作用项，V0G 起加对偶项。

| 轮次 | 改动 | 白化均匀误差 | e_A | 平滑能量最大误差 | 平滑逆响应能量误差 | 结论 |
|---|---|---|---|---|---|---|
| V0A | 首版，稀疏内部耦合，支撑加权探针 | 未测 | 63.6% | 21% | 无 | 碎片节点偏刚 258 倍，损失几乎不激励它们 |
| V0B | 加白化探针，学习率 3e-3 | 崩溃 | 无 | 无 | 无 | 稠密内部因子三角回代几何级放大 |
| V0C | 行对角占优内部因子 | 1.62 | 23.2% | 39% | 9.6 | 首次突破 38.8% 对角线；内部耦合与低秩头被退化方向压到零 |
| V0D | 消掉内部因子，幂迭代收缩约束 | 0.091 | 23.1% | 22% | 15.3 | 约束不严，Ŝ 失去正定，逆响应发散 |
| V0E | 精确正定 Woodbury 形式，稀疏耦合 | 停止 | 无 | 无 | 无 | 每步 3.8 秒，稀疏 Gram 反向太慢 |
| V0F | 同形式，稠密 512 列，20000 步 | 0.011% | 0.137% | 0.22% | 0.45 | 正向指标全部过关；一个碎片模态被软化到零，逆响应失真 |
| V0G | 从 V0F 续训 4000 步，加对偶项 | 0.12% | 0.137% | 0.21% | 0.043 | 对偶误差 24 → 0.0034；力控位移能量误差 0.17 |

V0G 终点的力控指标：光滑方向逆响应能量误差 4.3%，局部方向 7.9%，支撑加权光滑力位移 16.6%，柔度最大误差 6.0%，高斯随机力位移 37.6%。趋势单调下降，学习率已衰减到 3e-5。

判断：表示容量已证明充分，正向指标超过门槛两个数量级。剩余误差集中在小切碎片节点的逆响应，是优化时间问题，也是 V1 网络必须逐节点解决的问题。

关键教训：

1. 能量损失对"过软"有界，对"过刚"无界，单独使用会把个别模态软化到零。对偶项 ‖R*(BŜBᵀ)⁻¹R*ᵀξ − ξ‖² 用模型的精确受限逆，把过软惩罚变成无界，是必需的。
2. Schur 补只依赖 M = L_GI C_II⁻¹，内部因子不应是独立参数。(I + MMᵀ)⁻¹ 形式把软化写成闭式且严格正定。
3. 任何约束都不能靠幂迭代近似执行，优化器会把所有方向同时推到边界。
4. 大量共享参数的稠密三角因子在 Adam 下会失去对角占优，三角回代放大失控。

## V1：几何网络版本（多标签）

网络：六个面各一张 65×65 图，通道为解析场在深度 0、1、2、4 的切片、节点掩码、支撑度量、坐标和面编码，共 54 通道；四个空洞卷积残差块，感受野约半径 0.23；节点特征做 LayerNorm 后接三个头：对角头输出有界的对数修正，配对头在近场节点对上输出有界的 3×3 块，模态头每个自由度输出 512 列。参数 58.8 万。

标签：训练划分里选 32 张 q ≤ 26000 的 FULL 标签，教师因子用项目自带的 dense_reference 生成。项目代码禁止打开正式验证集，留出集取训练划分中未用的 6 张，另加 0401。

冒烟测试发现的两个问题及修正：

- 共享权重的相干更新让网络在 40 步内把整个算子软化到零，能量损失停在 1。修正：特征归一化、头输出用 tanh 限幅、能量损失权重在前 3000 步线性爬升、作用项从头保持。
- 对偶项在 L 条件恶化时爆到 1e64。修正：对偶误差用 tanh 软饱和到 1e4，梯度非有限时跳过该步。

修正后 400 步冒烟：e_A 59% → 46%，留出标签 45%，无发散。正式运行 V1A：26 张训练标签轮转，20000 步，每 3000 步在 3 张训练和 3 张留出标签上做完整评价。

## 当前门槛（9 月 12 日晚重定）

旧门槛全是探针指标，已废止。新门槛用谱等价常数和每模态散度，在训练与留出标签上以完整谱核对：

- 第一阶段：全部方向的刚度比 μ 落在 0.5 到 2 之间，每模态散度 D/d 小于 1e-2。
- 第二阶段：μ 落在 0.9 到 1.1 之间，D/d 小于 1e-3。

V1G 已按此启动：从 V1E 第 20000 步热启动，体积分支关闭，增广 0.5，散度权重 1，极端项权重 0.1，20000 步。

## V1C–V1E 结果与精确谱体检（9 月 12 日下午）

V1C（白化对偶）、V1D（off_scale 10）、V1E（物理载荷对偶 + 柔度项）三轮都收敛到同一个平台：训练与留出标签的 e_A 约 27%，能量探针误差 0.13–0.18，但逆响应指标在评价点之间大幅摆动（V1E 第 15000 步 phys_force 0.42–0.94，第 18000 步 1.36–1.77，柔度误差 0.06 → 0.98）。探针平均值无法解释这种摆动，于是写了不依赖任何探针的精确体检 `superelement/spectrum_eval.py`：

- 在 CPU 上以 float64 物化学生 Ŝ，取商空间 Â = BŜBᵀ，白化 W = R*⁻ᵀ Â R*⁻¹，做完整特征分解得到广义谱 μ。
- 报告 log-det 散度 D = Σ(μ − log μ − 1)、谱等价常数 μ_min、μ_max、分位数，并把每个模态按教师能量度量归类：滑尖节点位移占比、粗面空间占比、以及该模态"看到"的教师刚度 uᵀAu/uᵀu。
- 同时核对了两尺度学生的闭式 log det Â = 2Σlog L_ii − log det(I+MᵀM) + log det(NᵀŜ⁻¹N) − log det(NᵀN)（B 为正交商），与特征值和稠密 Cholesky 三者一致到 1e-10。

| 检查点 / 标签 | μ_min | μ_max | D/模态 | μ 的 10%–90% 分位 | 教师刚度 <1e-5 的模态数及其 D 占比 |
|---|---|---|---|---|---|
| V0G 4000 / 0415（单标签） | 0.145 | 3.62 | 4.5e-4 | 0.997–1.003 | 2398 个模态，占 D 的 99% |
| V1C 20000 / 0220 | 0.119 | 216 | 0.71 | 0.54–2.93 | 2102 个，85% |
| V1C 20000 / 0328（留出） | 0.128 | 670 | 1.83 | 0.46–3.85 | 1480 个，80% |
| V1C 20000 / 0434（留出） | 0.116 | 198 | 0.66 | 0.55–2.69 | 1678 个，82% |
| V1E 15000 / 0220 | 0.126 | 17.2 | 0.096 | 0.56–1.58 | 1469 个，33% |
| V1E 15000 / 0328（留出） | 0.044 | 30.1 | 0.18 | 0.52–1.93 | 454 个，31% |
| V1E 15000 / 0434（留出） | 0.051 | 12.3 | 0.093 | 0.56–1.57 | 1077 个，29% |

读法：μ 是学生相对教师在某个方向上的刚度比。μ_max = 670 表示 V1C 在某个方向把结构做硬了 670 倍，μ_min = 0.044 表示 V1E 在某个方向软了 23 倍。任何载荷的响应误差都由这两个常数界定，所以它们就是"物理可信"的证书。

结论：

1. V0G 的平均指标（白化误差 1e-4、对偶 0.0034）掩盖了 3.6 倍过硬和 7 倍过软的极端模态，全部落在教师最软的 2398 个模态里（薄材料处的局部软模态，不是滑尖）。平均型损失对这些模态的权重只有 1/d。
2. V1 系列的散度几乎全部来自教师最软的约 10% 模态；极端模态的峰值节点位于薄杆与面的交接处（0328 的 [12,32,64]、[64,12,32] 等对称位置）和真正的滑尖节点（w ≈ 0.003）。
3. V1E 的物理载荷对偶把 D/模态从 1.8 压到 0.18，但极端模态仍差 12–30 倍，且随训练来回摆动。
4. 对 V1E 检查点施加一个它没见过的立方对称帧（点反演），学生算子数值奇异（μ_min ≈ 1e-17、μ_max ≈ 1200）：off_scale 10 允许 L 完全失去对角占优，能否稳定完全由目标函数决定；数据增广必须从头开启。

## 监督 v2：不用探针、不引入粗空间的直接算子学习

目标函数只有两项，都是白化谱 μ 的酉不变函数，与探针选择无关，也不含任何粗空间：

- 每模态 log-det 散度 D/d，D = tr(A⁻¹Â) − log det(A⁻¹Â) − d = Σ(μ − log μ − 1)。它等于教师与学生弹性 Gibbs 测度之间的 KL 散度的两倍，是把学生当作能量模型对教师做最大似然。迹项精确可算：tr(A⁻¹Â) = ‖(I+MMᵀ)^{-1/2} L Zᵀ‖²_F，Z = R*⁻ᵀB 每个标签块算一次；log det Â 用上面的闭式。二阶展开就是白化 Frobenius 误差，过软方向的代价 −log μ 无界，过硬方向线性。
- 极端模态项：对 W 和 W⁻¹ 各维持一个 8 维块做热启动的块幂迭代（每步 2 次），Ritz 值用同一个 φ(μ) = μ − log μ − 1 进入损失。它盯住谱等价常数本身，也就是"对任何载荷都可信"的那两个数；单模块的谱等价常数在装配后原样保留（Loewner 序在合同变换与求和下不变），所以装配一致性由它自动保证，不需要另加装配损失。

已实现并验证（`superelement/v1_superelement.py` 的 `--divergence-weight`、`--extreme-weight`，`superelement/test_divergence.py`）：在 0328 上训练时公式给出的 D 与精确谱的 Σ(μ − log μ − 1) 一致到 1e-11（原始帧 2270.887，z 反射共轭帧 3310.533），40 步块幂迭代的 Ritz 极值与真实 μ_max、μ_min 一致到 12 位，梯度有限。教师 A 在该目标下不再需要装入显存，迹项以 float32 计算梯度。

评价改为精确量：每 3000 步报告 D/模态、log-det 差、块幂迭代给出的 μ_max 下界与 μ_min 上界；检查点用 spectrum_eval 做完整谱。旧的探针指标保留在日志里只作对照。退役的监督：高斯力探针、白化位移对偶、支撑加权随机细场、各向同性作用项、标量柔度项。

待启动：`superelement/run_v1g.sh`（从 V1E 热启动，增广 0.5，散度权重 1，极端项权重 0.1，20000 步）。
