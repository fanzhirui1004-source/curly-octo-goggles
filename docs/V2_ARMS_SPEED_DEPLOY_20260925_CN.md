# v2 对比臂、训练提速、部署速度与诊断结果（2026-09-25 夜间）

所有数字来自 RTX 5090 服务器（torch 2.8 / cu128），原始 JSON 在服务器 `/root/autodl-tmp/OPL/S1/V2/`。

## 1. 四个续训臂（均从 v1 snap_60000 续训 15k 步，EMA 选模）

恒等视角（现行规则）与旋转视角（eval_views：视角 0,5,17,29,38,46，20 个 val 几何）：

| 臂 | 恒等 force / support / face | 旋转均值 force / support / face | 族规则（≥3 族同向 & 最差类 ≤1.10） |
|---|---|---|---|
| c_ctrl | 3.9 / 4.6 / 2.7 % | 26.0 / 25.6 / 11.2 % | — |
| c_b2（B2 几何一致特征） | 3.9 / 4.5 / 2.6 % | 26.6 / 26.3 / 12.3 % | 2/4 族，中性 |
| c_b1（B1 knee 有界系数） | 3.9 / 4.6 / 2.7 % | 25.8 / 25.7 / 12.2 % | 2/4 族，中性；sat_max 全程 0（截断从未触发） |
| c_oh best（5k） | 4.9 / 5.8 / 3.4 % | 11.8 / 13.0 / 6.3 % | 0/4 族（恒等视角更差） |
| **c_oh last（15k）** | 5.1 / 5.9 / 3.8 % | **6.8 / 7.9 / 4.2 %** | — |

- O_h 增广把旋转视角误差降到约 1/4（最差视角 force 44% → 7.6%），15k 权重 6 个视角几乎持平且仍在改善；恒等视角代价约 +30%。与 2026-09-20 剂量实验（样本内代价、样本外收益）一致。
- 现行选模只看恒等视角，选中了 5k 权重（旋转视角差一倍）。**带 O_h 的 v2 必须改为多视角选模分数。**
- B1、B2 对恒等和旋转视角都中性；B1 在分布内等价于恒等映射（零代价的分布外保险）。
- 注意：对抗搜索（cuDSS fp32 Neumann 分解）不可逐位复现，单步 loss 在同种子重跑间可差 2–8%（t_evalcost），1–4% 的臂间差异需要同配置重跑对照才能下结论。

**暂定 v2**：O_h（多视角选模）+ B1；B2 可选；B3 由 A4 触发、待定；可选小臂：24 个真旋转代替 48。

## 2. A4 误差归因（v1 60k，5 个几何）

软载荷类（force/support/face）的误差能量中 ghost penalty 份额：重割 r1 60–68%，0029_r2 32–54%，0020_r2 face 48%，FULL 胞 force/face 39–42%；grf/macro 约 2%。弱单元组相对场误差（0.14–0.39）低于端口单元与其他单元 → B5 不触发；弱+GP ≥40% → **B3 触发**。平滑/粗糙分解在 r1 上病态（+13.4 / +14.0 / −26.4），不作依据。

## 3. A7 r1 y 161% 离群值

测试胞在该工况能量占比 w≈0.000，其灵敏度的相对误差分母近零；误差来自学习邻胞（该工况 w=1.0，ε≈2.3%）经界面传入。真正的问题是 step‑1 FULL 模型在"割胞带动的轻载边界"下 ε 达 17%。建议全学习格架附加指标加能量占比下限（门槛本身不变）。step‑2 权重重跑见 lat_full_s2。

## 4. TF32

代码从未设置，PyTorch 默认 `cudnn.allow_tf32=True`，U‑Net conv3d 实际调用 `…tf32f32…` 核（matmul 不是 TF32）。

| 几何 | 场相对差 | 能量误差相对差 | Ŝ 不对称 TF32 / fp32 | 前向 16 / Ŝq 16（TF32 → fp32） |
|---|---|---|---|---|
| FULL 0007 | 1.7e-5 | ≤0.07% | 4.1e-5 / 2.7e-7 | 110 → 112 ms / 199 → 213 ms |
| 重割 r1 | 3.5e-5 | ≤0.05% | 1.4e-3 / 7.6e-6 | 22 → 24 ms / 38 → 52 ms |

对精度无实质影响；对部署影响 Ŝ 对称性（CG 要求对称）。建议 v2 起 fp32 卷积（开关，默认值待定）。

## 5. 训练提速

c_ctrl 耗时构成（15k 步，92 min）：训练步 65%，换池 14%（其中首访对抗 496 s，每几何 3.35 s，长跑中摊薄），评估 21%（每次 388 s）。

- **评估开关**（train3 `eval_weights='sel'`、`cert_every`，默认关）：不改变轨迹与选模；中间评估省 40–50%。
- **融合 Triton 超边核**（`FUSED_HYPER=1`，默认关）：单层前向+反向 12.3 → 6.7 ms（列块 512，1.84×）；端到端训练步 r2 0.194 → 0.170 s（1.14×）、FULL 0.384 → 0.331 s（1.16×）；梯度差与基准自身重跑噪声同量级（r2 0.5% 对 0.5%，FULL 1.8e-4 对 1.7e-4），显存不变。列块 128 反而慢（0.87×），默认已改为 512。
- **按内核分类（FULL，每步 GPU 374 ms）**：逐元素 125 ms（33%）+ 拷贝/填零 70 ms（19%）> cuSPARSE 103 ms（28%）> GEMM 38 > 卷积 14。下一步：把钳位 `(X+dX)(1−pm)+X0·pm` 与残差加并入 scatter 核或用 torch.compile 融合逐元素（舍入级结果差，开关），预计再省 20–30%。
- 多 GPU（方向并行 DDP，同一几何的 16 个方向拆到多卡，数学等价）约 N× 近线性，需付费资源，由用户决定。

## 6. 部署速度（单独汇报）

每胞（FULL 0007，40 万 DOF / 2.6 万端口）：

| | 精确 CutFEM | 学习算子 |
|---|---|---|
| 公共：建网格+矩+组装 K | 3.7 s | 3.7 s（变分读出需要 K） |
| 分解 fp64 / fp32 | 8.6 s / 3.4 s | —（冻结 0.15 s） |
| 常驻显存 | 5.1 / 0.65 GB | 1.37 GB（CSR）/ 0.34 GB（融合） |
| Ŝq B=1 / 16 / 64 | 73 / 100 / 316 ms | 15 / 199 / 742 ms（融合 15 / 173 / 646） |
| 能量比 | 1 | 1.008 均值，最大 1.015（其余三胞最大 2.7%） |

格架（r2 + FULL，3.6 万自由度，9 载荷）：

| 算子 | 预条件 | 迭代 | 每次迭代 | 总时间 |
|---|---|---|---|---|
| 学习 | 理想（稠密精确，不可部署） | 13 | 5.0 s | 65 s |
| 学习 | 无 | 3000 未收敛 | 0.15 s | — |
| 学习 | Jacobi | 2524 | 0.15 s | 387 s |
| 精确分块 | Jacobi | 2149 | 0.16 s | 350 s |

结论：学习算子目前的优势是省分解（每胞 3–9 s）与显存（最多 15×），单方向 Ŝq 快 5×；多方向时慢约 2×。格架求解的瓶颈是可部署预条件器（上千次迭代），两条路线都一样。部署提速优先级：(1) 两层/代数多重网格预条件（期望 ~50×）；(2) 无组装 K 乘去掉组装；(3) 融合核 + fp32 卷积（Ŝ 对称、显存 1/4）。
