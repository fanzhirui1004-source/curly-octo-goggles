 Claims ( 10 ) 1.一种超复杂结构的力热耦合仿真和结构优化方法，其特征在于，包括： 1)对于给出的模型和设计域，生成和建立后续仿真和优化所需要的参数和数据结构；包括构造背景粗网格，构造种子点和半径，基于voronoi结构建模多孔结构，并对其应力场进行预计算； 2)计算密度场，根据得到的密度场，基于背景粗网格和形状函数对生成的多孔结构进行热力学仿真；在热力学仿真过程中，计算出相关的目标函数和约束条件的参数值和灵敏度； 根据得到的应力场，查询每个种子点对应的应力矩阵，对应力矩阵进行特征分解及提取处理，从而得到该种子点对应的权重矩阵，针对多孔结构计算表征结构各向异性的目标函数值和灵敏度； 使用拓扑优化方法，根据灵敏度通过GCMMA方法更新控制参数，从而更新多孔结构，优化材料分布； 3)重复步骤2)，直到控制参数变化小于阈值，当上述过程完成后，得到最终的种子点和半径，以及多孔结构的拓扑信息，利用上述结构基于有向距离场构建出最终优化出的多孔泡沫。 2.根据权利要求1所述的超复杂结构的力热耦合仿真和结构优化方法，其特征在于，步骤1)中通过基于四面体的网格剖分构造出背景粗网格，对该模型采用基于体素法的拓扑优化方法对应力场进行预计算，得到引导各向异性优化的应力场。 3.根据权利要求1所述的超复杂结构的力热耦合仿真和结构优化方法，其特征在于，步骤1)中将模型抽象成一个由种子点的位置和半径控制的梁结构网络，生成和初始化用于构建多孔结构的种子点，通过构造出的背景粗网格、种子点和半径，在设计域内生成Voronoi泡沫结构，从而建模出由多个具有厚度的梁组成的多孔结构。 4.根据权利要求3所述的超复杂结构的力热耦合仿真和结构优化方法，其特征在于，构造隐式函数来表示Voronoi泡沫结构，开放式Voronoi泡沫被定义为限定在模型内部的Voronoi细胞具有一定半径的边缘；将Voronoi边缘引入的所有梁的隐式函数混合在一起，或者与外壳一起，得到定义为密度场的Voronoi泡沫的隐式表示；具体的： 每个Voronoi泡沫Ω V (X,r)由两部分组成：内部部分和边界部分； 对于内部部分，在边界框中，使用外部自由形状剪切其所有边缘，剩余第j条边变成半径为边j相邻种子点平均值的梁，对应于顶点v 1 ，v 2 的Voronoi边，定义梁φ，由一个半径为 高度为||v 1 -v 2 ||的圆柱体和两个半径为 的半球端组成，梁φ的隐式形式 为： 式中d(x,v 1 ,v 2 )表示点x到边v 1 ,v 2 的最小距离， 其中 a＝v 2 -v 1 ,b＝x-v 1 ,e＝x-v 2 , 整个开孔Voronoi泡沫的隐式表示是使用Kreisselmeier-Steinhauser(KS)函数将平滑后的所有梁的隐式函数合并, 式中φ max ＝max(φ 1 ,…,φ n )，n为梁数，p取16； 与外部形状Ω相关的边界部分也以隐式形式φ Ω (x)表示； Voronoi泡沫Ω V (X,r)最终被正则化Heaviside函数H(Φ(x))描述为密度场： 其中∈控制正则化的大小，α＝1e -6 。 5.根据权利要求4所述的超复杂结构的力热耦合仿真和结构优化方法，其特征在于，步骤2)中，为了实现隐式泡沫的仿真，将模型浸入固定的四面体背景网格D＝{D e ,e＝1,2,…,N}中覆盖形状Ω，即 在每个四面体元素D e 中，密度H e 设为其四个节点上H(x)的平均值， 式中， 为单元D e 中第k个节点的坐标； 考虑一个具有固定位移边界Γ u 和固定温度边界Γ T 的设计域Ω，表面力作用在诺伊曼边界Γ N 上，表面热通量位于边界Γ h 上； 则线性热弹性连续体在机械和温度载荷作用下的静力平衡方程表示为： K m U＝F＝F m +F th 有 K m ＝∫ Ω B T DBdΩ F th ＝∫ Ω B T Dε th dΩ 其中K m 为刚度矩阵，U为节点位移矢量，F m 为机械载荷矢量，F th 为热载荷矢量；B为应变位移矩阵，N为形状函数矩阵；ε th 为热应变向量； K m 由单元刚度矩阵K m,e 组合而成 K m ＝∑ e K m,e 式中B e 为单元应变-位移矩阵，Ω e 为单元物理域； 热载荷F th 由单元热载荷F th,e 组合而成 F th ＝∑ e F th,e ε th ＝α(T e -T ref )Φ T 式中ε th,e 为单元热应变向量，α为热膨胀系数，T e 为单元温度，是单元节点温度T e 的平均值，T ref 为热膨胀参考温度，Φ为三维空间中的向量[1 1 1 0 0 0]，同样，通过节点位移U的插值，得到背景网格D中的位移u(x)， u(x)＝N e (x)U e 式中N e (x)为单元节点上的形状函数矩阵，U e 为单元节点位移； 将材料中产生的热应力纳入力学方程，稳态热传导的热平衡方程为K T (X,r)T＝P 式中K T 为导热矩阵，T为节点温度矢量，P为热流密度矢量，K T 和P由单元导热矩阵K T,e 和单元热流向量P e 组合而成，有 K T (X,r)＝∑ e K T,e (X,r) P＝∑ e P e 式中λ e 为有效导热系数， 通过插值节点温度T，得到背景网格D中的温度T(x), T(x)＝N e (x)T e 。 6.根据权利要求5所述的超复杂结构的力热耦合仿真和结构优化方法，其特征在于，Voronoi泡沫设计问题采用种子X和半径r作为性能优化的设计变量，以测量物体弹性势的柔度C(X,r,U)为物理目标， C(X,r,U)＝F T U 并通过形状调节能量S(X)将Voronoi细胞规整成近似规则多面体 即每个Voronoi细胞 的种子X i 与质心 之间的欧氏距离，经权值矩阵 加权后的和； 对于具有体积和温度约束的优化问题，求出优化设计参数p＝{p j ,j＝1,2,…,n}，其中p j 表示变量X或r的一个分量： 其中目标函数J(p,U)设为物理目标与形状正则化项的加权和 J(p,U)＝(1-w)C(p,u)+w S(X), V和 为结构总体积和最大体积约束，T i 和 为节点温度和相应的最大温度约束，p j 和 为设计变量p j 的下界和上界； 利用优化目标和约束相对于设计变量p的梯度，采用基于数值梯度的全局收敛移动渐近线方法GCMMA求解该优化问题。 7.根据权利要求6所述的超复杂结构的力热耦合仿真和结构优化方法，其特征在于，柔度C对设计参数p j 的梯度为： 有 其中 温度相对于设计参数的梯度为 8.根据权利要求6所述的超复杂结构的力热耦合仿真和结构优化方法，其特征在于，对于每个Voronoi单元 其中x点处的权值矩阵 的计算如下： 基于体素模型仿真和优化得到的应力场，应力场中x点处的柯西应力张量有 其中σ ii 为正应力，σ ij ,i≠j为剪应力，由于剪切应力的对称性，σ ij ＝σ ji ，故σ x 为对称矩阵，对其特征值进行分解： σ x ＝VΛV -1 其中Λ是一个对角矩阵，其特征值在对角线上，V是一个矩阵，其特征向量作为其列，提取最大特征值对应的特征向量作为主方向向量v m ： v m ＝[v 1 v 2 v 3 ] M x 由v m 的组件组成: 9.根据权利要求6所述的超复杂结构的力热耦合仿真和结构优化方法，其特征在于，引入材料感知的CBN形状函数来取代原有的形状函数，以减小自由度加快计算速度，具体的：对于粗网格划分的整个虚拟域，每个粗化单元e及其虚域Ω e ，进一步将其划分为一个四面体网格，得到e的精细化网格h； 将单元形状函数N e 通过CBN变换矩阵 替换为由细网格上的线性形状函数组成的单元形状函数： 其中N e,h (x)为Ω e 细网格上节点形状函数的集合； 应变位移矩阵B e 替换为 式中B e,h 为Ω e 细网格的全局应变位移矩阵；CBN变换矩阵 旨在将CBN节点值映射到Ω e 中的内部值，由边界插值矩阵Ψ与边界-内部变换矩阵 的乘积得到，如下： 其中Ψ和 将CBN的位移映射到边界节点，然后映射到Ω e 中的全精细节点；同样，在仿真中，单元导热矩阵的梯度计算为 同理，K m,e ,F th,e 及其梯度替换为： 和 式中K m,e,h 和F th,e,h 分别为Ω e 细网格的高保真刚度矩阵和热载荷。 10.一种超复杂结构的力热耦合仿真和结构优化系统，其特征在于，用于实现如权利要求1-9任一项所述的方法。 