 Claims ( 10 ) Translated from Chinese 1.一种用于芯片多物理场仿真的高阶多尺度有限元方法，其特征在于，包括： 1. A high-order multi-scale finite element method for chip multi-physics field simulation, characterized by comprising: 将所设计的芯片模型嵌入在规则的粗背景网格中，基于边界插值函数对粗单元的病态粗节点进行判断； The designed chip model is embedded in a regular coarse background grid, and the ill-conditioned coarse nodes of the coarse unit are judged based on the boundary interpolation function; 在粗单元表面移除病态边界插值函数，构建缩减粗单元，在粗网格边界上的细节点采样得到粗节点-边界温度变换矩阵； Remove the ill-conditioned boundary interpolation function on the coarse element surface, construct a reduced coarse element, and sample the detail points on the coarse grid boundary to obtain the coarse node-boundary temperature transformation matrix; 通过求解粗单元内的芯片温度场子结构问题，建立边界细节点到内部细节点的温度映射关系，得到边界-内部温度变换矩阵； By solving the chip temperature field substructure problem in the coarse unit, the temperature mapping relationship from the boundary detail point to the internal detail point is established, and the boundary-internal temperature transformation matrix is obtained; 基于粗节点-边界温度变换及边界-内部温度变换得到粗节点-细节点温度变换矩阵，对每个粗单元构建数值稳定的反映内部材料分布的数值形函数，用于对所述芯片模型进行嵌入式仿真。 A coarse node-detail point temperature transformation matrix is obtained based on coarse node-boundary temperature transformation and boundary-internal temperature transformation, and a numerical shape function reflecting internal material distribution is constructed for each coarse unit for embedded simulation of the chip model. 2.根据权利要求1所述的用于芯片多物理场仿真的高阶多尺度有限元方法，其特征在于，判断粗单元 中一个粗节点 是否为病态节点的方法如下：如果粗节点 对应的边界插值矩阵 的最大值小于给定阈值，即 2. The high-order multi-scale finite element method for chip multi-physics field simulation according to claim 1, characterized in that the coarse element A thick node The method to determine whether it is a pathological node is as follows: if the rough node The corresponding boundary interpolation matrix The maximum value of is less than the given threshold, that is ， , 其中 为给定阈值，那么 对于粗单元 是病态粗节点。 in For a given threshold, then For coarse elements It is a pathologically thick node. 3. 根据权利要求1所述的用于芯片多物理场仿真的高阶多尺度有限元方法，其特征在于，所述构建缩减粗单元的方法如下：对粗单元 的每个边界面 ，将粗节点分类为位于边上的角点和位于面内的非角点，其下标集合分别记为 和 ，保留所有角点 ，以及剔除病态非角点后所有良态的非角点，其下标集合记为 。 3. The high-order multi-scale finite element method for chip multi-physics field simulation according to claim 1, characterized in that the method for constructing the reduced coarse element is as follows: Each boundary surface , classify the coarse nodes into corner points on the edge and non-corner points in the surface, and their subscript sets are recorded as and , keep all corner points , and all the well-conditioned non-corner points after removing the ill-conditioned non-corner points, the subscript set is recorded as . 4.根据权利要求3所述的用于芯片多物理场仿真的高阶多尺度有限元方法，其特征在于，所述面 上任意一点 的温度通过角点 和保留的非角点 进行插值， 4. The high-order multi-scale finite element method for chip multi-physics field simulation according to claim 3, characterized in that the surface Any point on The temperature passes through the corner and the retained non-corner points To interpolate, ， , 其中，ij为粗节点下标， 是二维伯恩斯坦基函数， 是角点上的插值基函数，将粗单元 的插值函数写成矩阵形式 ，遍历所有边界细节点 ， 为边界细节点数目，得到粗节点-边界温度变换矩阵 ， Among them, ij is the coarse node subscript, is the two-dimensional Bernstein basis function, is the interpolation basis function at the corner point, and the coarse element The interpolation function is written in matrix form , traverse all boundary detail points , is the number of boundary detail points, and the coarse node-boundary temperature transformation matrix is obtained , ， , 其中 为边界面F内下标为ij的粗节点的温度向量，该边界面F内所有粗节点温度向量装配构成粗单元 上的温度向量 。 in is the temperature vector of the coarse node with the subscript ij in the boundary surface F. All the coarse node temperature vectors in the boundary surface F are assembled to form a coarse unit. The temperature vector on . 5.根据权利要求1所述的用于芯片多物理场仿真的高阶多尺度有限元方法，其特征在于，通过求解粗单元 内的芯片温度场子结构问题，将边界细节点温度 映射到内部细节点温度 ，其以粗节点-边界温度变换为温度约束，平衡方程为： 5. The high-order multi-scale finite element method for chip multi-physics field simulation according to claim 1, characterized in that by solving the coarse element The chip temperature field substructure problem is to convert the boundary detail point temperature Mapping to internal detail point temperature , which uses the coarse node-boundary temperature transformation as the temperature constraint, and the equilibrium equation is: ， , 其中 是传热矩阵的子矩阵， 和 是热载荷的子向量，通过求解上式第二行的 , 同时假定 ，得到： in is a submatrix of the heat transfer matrix, and is the subvector of the heat load, which is obtained by solving the second line of the above equation , and assume that ,get: ，其中 ， ,in , 其中 是细节点温度向量， 是单位矩阵， 是所计算的边界-内部温度变换矩阵。 in is the temperature vector of the detail point, is the identity matrix, is the calculated boundary-to-interior temperature transformation matrix. 6.根据权利要求1所述的用于芯片多物理场仿真的高阶多尺度有限元方法，其特征在于，在粗单元 中，数值形函数被表示成细网格上的分片线性形函数 ： 6. The high-order multi-scale finite element method for chip multi-physics field simulation according to claim 1, characterized in that in the coarse element In the example, the numerical shape functions are represented as piecewise linear shape functions on a fine grid. : ， , 其中 是细网格上的线性形函数， 是粗节点-细节点温度变换矩阵，也就是粗节点-边界映射 和边界-内部映射 的乘积， in is a linear shape function on the fine grid, It is the coarse node-detail point temperature transformation matrix, that is, the coarse node-boundary mapping and boundary-interior mapping The product of 。 . 7.一种数值稳定的高阶多尺度有限元仿真系统，其特征在于，包括： 7. A numerically stable high-order multi-scale finite element simulation system, characterized by comprising: 节点判断模块，用于输入待仿真模型，嵌入粗背景网格中，基于边界插值函数对模型粗单元的病态粗节点进行判断； A node judgment module is used to input the model to be simulated, embed it into the coarse background grid, and judge the ill-conditioned coarse nodes of the coarse units of the model based on the boundary interpolation function; 缩减粗单元构建模块，根据节点判断模块输出的病态粗节点，构建缩减粗单元，在粗单元表面保留位于边上的全部角点和位于面内的良态非角点，并在粗网格边界上的细节点采样得到粗节点-边界温度变换矩阵； The reduced coarse unit construction module constructs the reduced coarse unit according to the ill-conditioned coarse nodes output by the node judgment module, retains all corner points on the edge and well-conditioned non-corner points on the surface of the coarse unit, and samples the detail points on the coarse grid boundary to obtain the coarse node-boundary temperature transformation matrix; 子结构问题求解模块，通过求解粗单元内的芯片温度场子结构问题，建立边界细节点到内部细节点的温度映射关系，得到边界-内部温度变换矩阵； The substructure problem solving module solves the substructure problem of the chip temperature field in the coarse unit, establishes the temperature mapping relationship from the boundary detail points to the internal detail points, and obtains the boundary-internal temperature transformation matrix; 数值形函数确定模块，基于缩减粗单元构建模块输出的粗节点-边界温度变换矩阵及子结构问题求解模块输出的边界-内部温度变换矩阵得到粗节点-细节点温度变换矩阵，对每个粗单元构建数值稳定的反映内部材料分布的数值形函数； The numerical shape function determination module obtains the coarse node-detail point temperature transformation matrix based on the coarse node-boundary temperature transformation matrix output by the coarse element construction module and the boundary-internal temperature transformation matrix output by the substructure problem solving module, and constructs a numerical shape function that reflects the internal material distribution for each coarse element in a numerically stable manner; 仿真模块，基于所得到的数值形函数对所述模型进行嵌入式仿真。 A simulation module performs embedded simulation on the model based on the obtained numerical shape function. 8.一种电子设备，其特征在于，包括： 8. An electronic device, comprising: 一个或多个处理器； one or more processors; 存储器，用于存储一个或多个程序； A memory for storing one or more programs; 当所述一个或多个程序被所述一个或多个处理器执行，使得所述一个或多个处理器实现如权利要求1-6任一项所述的方法。 When the one or more programs are executed by the one or more processors, the one or more processors implement the method according to any one of claims 1 to 6. 9.一种计算机可读存储介质，存储有计算机可执行指令，上述指令在被执行时用于实现权利要求1至6中任一项所述的方法。 9. A computer-readable storage medium storing computer-executable instructions, wherein the instructions are used to implement the method according to any one of claims 1 to 6 when executed. 10.一种用于力学仿真的数值稳定的高阶多尺度有限元方法，其特征在于，包括： 10. A numerically stable high-order multi-scale finite element method for mechanical simulation, characterized by comprising: 将带有位移和载荷边界条件的弹性材料模型嵌入在规则的粗背景网格中，基于边界插值函数对粗单元的病态粗节点进行判断； The elastic material model with displacement and load boundary conditions is embedded in a regular coarse background grid, and the ill-conditioned coarse nodes of the coarse element are judged based on the boundary interpolation function. 在粗单元表面移除病态边界插值函数，构建缩减粗单元，在粗网格边界上的细节点采样得到粗节点-边界位移变换矩阵； Remove the ill-conditioned boundary interpolation function on the coarse element surface, construct a reduced coarse element, and sample the detail points on the coarse grid boundary to obtain the coarse node-boundary displacement transformation matrix; 通过求解粗单元内的子结构问题，建立边界细节点到内部细节点的位移映射关系，得到边界-内部位移变换矩阵； By solving the substructure problem in the coarse unit, the displacement mapping relationship between the boundary detail points and the internal detail points is established, and the boundary-internal displacement transformation matrix is obtained; 基于粗节点-边界位移变换及边界-内部位移变换得到粗节点-细节点位移变换矩阵，从而对每个粗单元构建数值稳定的反映内部材料分布的数值形函数，用于对所述模型进行嵌入式仿真。 Based on the coarse node-boundary displacement transformation and the boundary-internal displacement transformation, a coarse node-detail point displacement transformation matrix is obtained, so as to construct a numerical shape function reflecting the internal material distribution for each coarse element, which is used for embedded simulation of the model. 